node_modules/
dist/
docs/.vitepress/cache/
docs/.vitepress/dist/
bk/

# .companies/ は業務データだがGit管理対象（PRフローで管理）
# .companies/ は .gitignore に含めない

# .companies/.active はローカル設定（各ユーザーが独立して組織切替するため）
.companies/.active
