# domain-tech-collection 活動レポート — 2026-08-16（日）

> 生成日時: 2026-08-16 15:37 JST（**2 回目・15:10 の初版を更新**） | 生成者: SAS-Sasao | 期間: 2026-08-16（today）

## エグゼクティブサマリー

**8/9 週の AI 配信解析**を主軸に、解析レポート 7 本の GitHub Pages 公開基盤と `/company-insights-cycle` Skill の新設まで到達した。ユニーク 349 件の記事から **20 件を curl で原文精読**し、記事の主張を自組織の実測値と突き合わせている。

**本日の最大の成果は、記事から得た知見より「自組織を測って出た事実」の方だった。** `AGENTS.md` が Codex 現用中に 2 ヶ月更新されていないこと、`plugins/` に無い Skill が 3 種あること、CLAUDE.md の Skill 台帳が実体と 5 件ずれていたこと（是正済み）は、いずれも記事を読んだことで探しに行って見つかった。

**同時に、自分の作業で 1 件の事実誤認を出した。** `git fetch` を怠った古いチェックアウトを実査し、実在する 7 日分・235 件のダイジェストを「未生成」と報告した。訂正 PR #784 で公開済みレポート・カタログ・README を修正し、再発防止として `/company-insights-cycle` Phase 0 に `git fetch` を必須手順化した。**この失敗モードは `/company-digest-insights` Phase 0 に既に明記されていたが、`/company` 経由で実行したため踏まなかった。**

未完了の懸念は、**採用候補が AD〜AU の 12 件たまって 1 件も着手していない**こと。特に優先度「高」の AO（CLAUDE.md を削るのをやめて整合性是正へ振り替え）と AD（AGENTS.md の畳み込み）は根拠が実測で揃っている。

## 活動統計

| 指標 | 値（15:10 初版 → 15:37 更新） |
|------|-----|
| セッション数 | 5 → **8 回** |
| ツール実行数（合計） | 379 → **854 回**（bash 643 / write 137 / read 55 / other 19） |
| 作成・編集ファイル数 | 21 → **24 件** |
| 完了タスク数 | 3 → **4 件** |
| main コミット数 | 7 → **13 件** |
| マージ済み PR | 3 → **6 件** |
| 新規オープン Issue | 2 件 |

## 完了タスク

- **[agent-teams-actions]** 日次ダイジェスト（GitHub Actions 経由）→ `docs/daily-digest/2026-08-16.md`
- **[direct]** 8/9 週の AI 配信解析（Claude Code / Codex / エージェント設計）→ `docs/insights/analyses/2026-08-w2-claude-code-codex-agent.md`（PR #781、Issue #782）
- **[direct]** `/company-insights-cycle` 初回通し実行 — 2026-08-10〜08-15 の追補解析 → 同レポート §8（PR #784）
- **[direct]** `/company-cycle today`（1 回目）→ 本レポート初版 + dashboard（PR #785）

## 進行中タスク

なし。

## 作成・更新された成果物

### 知見解析（`docs/insights/`）— 本日の主軸

| 種別 | 件数 | 備考 |
|---|---|---|
| 解析レポート MD | 7 本 | 組織スコープ → リポジトリルート `docs/` へ移設 |
| 生成 HTML | 8 ファイル | 各レポート + 一覧ページ（243.5 KB） |
| カタログ・README | 2 ファイル | 候補 AD〜AU を追加、A〜AU の 47 項目に |

### 基盤（`docs/` / `.claude/`）

- `docs/index.html` — 「知見解析レポート」カードを追加
- `.claude/hooks/generate-insights-analyses-html.sh` — 新規（MD→HTML 変換、依存ライブラリなし）
- `.claude/hooks/generate-dashboard.sh` — 同カードの定義を追加（再生成で消えないように）
- `plugins/cc-sier/skills/company-insights-cycle/` — 新規 Skill（`.claude/skills/` へ同期済み）
- `.claude/rules/artifact-placement.md` / `git-workflow.md` — 実配置に合わせて更新
- `CLAUDE.md` — Skill 台帳を 12 → **18 種**へ是正（表と実体が一致）

### 日次ダイジェスト

- `2026-08-12.md` / `2026-08-16.md`（GitHub Actions 生成）

## Issue 動向

### 新規オープンの Issue（期間内）

- **#782** [domain-tech-collection] 8/3-8/9 の AI 配信解析 — 訂正コメントを追記済み
- **#783** [domain-tech-collection] インタラクションログ 2026-08-16（自動）

### クローズされた Issue（期間内）

なし。

### マージされた PR（期間内）

- **#787** task-log テンプレートから `## reward` 見出しを除去
- **#786** reward プレースホルダが評価を塞いでいた問題を是正
- **#785** today レポートを生成（本レポート初版）
- **#784** 対象期間の誤りを訂正 + 2026-08-10〜08-15 を追補解析
- **#781** AI 配信解析 + 解析レポートの Pages 公開 + `/company-insights-cycle`
- **#772** 日次ダイジェスト 2026-08-12（Actions 自動）

## 会話トピック

### セッション別の依頼内容

- **セッション 9637b4fe**（164 発言 / 286 応答 / 312 ツール実行）
  - 8/9 週の AI 関連配信のうち、使えそうなもの（Claude Code / Codex / AI エージェント設計）を深堀解析して MD にまとめる
  - 解析レポートの配置先を `docs/insights/analyses/` に変更（過去分 6 本も含めて移設）
  - 配置ルール（`artifact-placement.md`）を実態に合わせて更新
  - 解析レポートを GitHub Pages に公開し、トップページにカードリンクを追加
  - `/company-cycle` を模倣した Skill を新設し、公開まで一気通貫にする
  - マージ後のデプロイ確認
  - 対象期間の誤りが判明 → 案 A（8/10〜8/15 の追補解析）を選択
  - `/company-cycle` の実行

### ファイル生成を伴わなかったやり取り

- 配置先の確認（`/docs/insights/analyses` が組織スコープ側かリポジトリルート側かの判断）
- 訂正の方針確認（8/3〜8/9 のままにするか、8/10〜8/15 を追補するか）

## 本日の判断と、その根拠

| 判断 | 根拠 | 記録先 |
|---|---|---|
| 解析レポート 7 本を `docs/` 側へ移設 | オーナー判断。ブラウザから読ませたい。`docs/guide/*.md` と同じ扱い | `artifact-placement.md` §知見解析レポートの例外 |
| カタログ・README は組織スコープに残置 | 採用可否は組織の意思決定であり、公開対象ではない | 同上 |
| MD を直接 Pages 配信しない | Jekyll 設定がなく front matter の無い `.md` は生テキストで返るため | `generate-insights-analyses-html.sh` |
| カード定義を 2 箇所に置く | `docs/index.html` は `/company-dashboard` が丸ごと再生成するため、片方だけでは消える | `generate-dashboard.sh` にコメント記載 |
| Agent Plugins 1.0.0 準拠は見送り | Claude Code 自体が標準に非対応。準拠しても得るものがない | catalog 候補 AT（❌ 不採用） |
| claudex 導入は見送り | 非公式構成でアカウント停止事例あり（誤検知として解除済みだが再発保証なし） | catalog 候補 AU |

## 追記（15:10 以降）— reward 評価の欠陥を発見・是正

初版レポートのマージ後、task-log に書いた「reward は post-merge hook が補完する」という前提を確認したところ、**その hook が存在しなかった**。

### 失敗の連鎖

1. `.claude/rules/task-log.md` のテンプレートが `## reward` の下に「（post-merge hook が自動追記）」を書くよう指示していた
2. **その post-merge hook は実在しない**（`daily-insights-sync.py` が `chore: post-merge` というコミットメッセージを除外しているだけ）
3. **`skill-evaluator.sh` は `^## reward` の有無でべき等判定する** → プレースホルダだけで「評価済み」と見なされスキップ
4. `rebuild-case-bank.sh` は `score:` を探すが見つからず `reward: None`

**テンプレートに従うほど reward が付かなくなる**構造だった。

### 実測と是正

| 項目 | 値 |
|---|---|
| プレースホルダで評価が塞がれていた task-log | **31 件** |
| Case Bank の reward null | **38 / 239 件（15.9%）** |
| 是正した件数 | 2 件（`ai-week-analysis` **null → 1.0** / `cycle-today` **0.2 → 1.0**） |

`cycle-today` の 0.2 は別の失敗モードで、**タスクが `in-progress` のうちに evolve が評価を走らせた**ため（`completed: false`）。べき等なので完了後も自動では直らなかった。

### 2 段階の修正が必要だった

| PR | 内容 | 十分だったか |
|---|---|---|
| #786 | ルール記述を実態へ修正 + 2 件の reward 是正 + Case Bank 再構築 | ❌ **不十分**。原因を書いただけで、テンプレートに `## reward` 見出しが残っていた |
| #787 | **テンプレートから見出しを除去**し、書かない理由と正しい手順を明記 | ✅ サンドボックスで `score` が追記されることを検証 |

### 本サイクルでの検証

本 cycle の task-log（`20260816-153503-cycle-today`）は **PR #787 の新テンプレートに従い `## reward` を書いていない**。Phase 1.5 の evolve でスコアが正しく付くかを実データで確認する（結果は task-log に記録）。

### 副産物 — 候補 AV のリスク範囲の訂正

`skill-evaluator.sh` は `started` が対象日のものだけを走査すると判明した。

```bash
[[ "$started" != "$today" ]] && continue
```

したがって**通常実行で過去 29 件が再評価されることはない**。当初「一括再評価は危険」と書いた範囲は実態より広く、リスクは「意図的に過去日を指定した場合」に限定される。

---

## 本日見つかった「台帳と実体のドリフト」4 件

本日の解析テーマ（§5.13 候補 AO「効くのは量ではなく整合性」）が、**外部記事の知見よりも自組織の実測として繰り返し現れた**。

| # | 内容 | 対処 |
|---|---|---|
| 1 | `AGENTS.md` が Codex 現用中に 2 ヶ月分岐（注意事項 28 件 vs 0 件） | ⬜ 候補 AD |
| 2 | CLAUDE.md の Skill 台帳が 12 種記載 / 実体 17 種 | ✅ 18 種へ是正（`plugins/` 未同期 3 種は残・候補 AE） |
| 3 | `artifact-placement.md` と実配置の食い違い | ✅ 例外として明文化 |
| 4 | `rules/task-log.md` が実在しない post-merge hook に依存 | ✅ #786 / #787 で是正（既存 29 件は候補 AV） |

**4 件とも、外部記事を読んだことをきっかけに自組織を測って見つかった。** 記事そのものの知見より、測って出た事実の方が本日の成果として重い。

---

## 次のアクション候補

1. **候補 AO + AD をセットで着手**（根拠: CLAUDE.md 208 行 + rules 1,010 行という実測が、1,650 回実験の「長さは効かない」範囲に収まった。削る作業をやめ、2 ヶ月分岐した `AGENTS.md` の畳み込みに振り替える）
2. **候補 AE の残り — `plugins/` に無い Skill 3 種の移送**（根拠: `company-evolve` / `company-quality-setup` / `company-report` が VCS 真ソース原則の外にある。CLAUDE.md の台帳側は本日是正済み）
3. **候補 AP — `deny` の具体設計を導入**（根拠: `settings.local.json` は allow のみ。`node -e` / `sh -c` / `git -c` の迂回経路という具体知見が得られた）
4. **候補 AV — reward null 29 件の復旧方針を決める**（根拠: Case Bank の 15.9% が評価不能。通常実行では再評価されないと判明したため、意図的な復旧手順を設計する必要がある）
5. **生成 HTML のブラウザ目視**（根拠: 構造検査は通したが、レイアウト・ダークモード・表の横スクロールを一度も見ていない）
6. **`.mcp.json` の `aws-iac-mcp-server@latest` をピン留め**（根拠: CLAUDE.md 自身のルール（#569/#570 の教訓）に反している。本日は指摘のみで対処せず）

## 本レポートで確認していないこと

- `.session-summaries` の `files_written` が **5 セッションとも 0 件**だった。フィールドが機能していない可能性があるが**未調査**。本レポートの「21 件」は `git log` 側の値
- 人間の発言数 164 はツール結果ターンを含む可能性があり、**実際の依頼件数とは一致しない**
- 本日新設した `/company-insights-cycle` の `--publish-only` / `--no-pr` フラグとエラー時中断は**未検証**
- **PR #787 の新テンプレートが本番で機能するかは、本サイクル（2 回目）の evolve 結果を見るまで未確定**。検証はサンドボックスのみ
- 他組織（`jutaku-dev-team` / `standardization-initiative`）の task-log は**調べていない**
- 採用候補は A〜AV の 48 項目。**未着手 13 件**

---
_このレポートは /company-report Skill によって自動生成されました_
_情報源: .session-summaries/ | .task-log/ | docs/ | GitHub Issues | .conversation-log/_
