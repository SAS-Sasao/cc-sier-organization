# domain-tech-collection 活動レポート — 2026-08-08（土）

> 生成日時: 2026-08-08 19:50 JST | 生成者: SAS-Sasao | 期間: 2026-08-08（当日）
> ※ 本レポートは 11:00 に生成した午前版を、午後の活動を含む 1 日分に更新したものです

## エグゼクティブサマリー

**外部知見の取り込みを「その場限りの解析」から「継続できる仕組み」に変えた日**。

午前は 1 記事を解析して実測し、ルールを 1 件追加した。午後は 8 月分の日次ダイジェスト **247 件（ユニーク 212 件）**を徹底解析し、その学習プロセス自体を `/company-digest-insights` として Skill 化した。さらに散在していた知見文書を `docs/insights/` に集約し、README を入口として整備した。

一連の流れで一貫していたのは「**主張を鵜呑みにせず自組織で実測する**」ことで、CLAUDE.md 37,309 字・強い言葉 78 箇所・Skill トリガー語重複 0 といった数値が得られた。これらは今後の採用判断の基準になる。

## 活動統計

| 指標 | 値 |
|------|-----|
| セッション数 | 4 回 |
| ツール実行数 | 140 回 |
| 完了タスク数 | 5 件 |
| マージ PR 数 | 4 件 |
| 新規 Issue 数 | 1 件 |
| 作成・編集ファイル数 | 14 件 |

ツール内訳: bash 107 / write 33

## 完了タスク

| モード | タスク | 成果物 |
|---|---|---|
| direct | AI が書くコメントの分量 解析 + 自組織での実測 | `analyses/comment-density.md` / CLAUDE.md 1 行 / 測定ツール / PR #753 |
| direct | `/company-cycle`（午前） | レポート / Case Bank 220 件 / dashboard |
| direct | 8 月ダイジェストの AI 関連ネタ徹底解析 + Skill 新設 | `analyses/2026-08-digest-ai.md` / `/company-digest-insights` / PR #757 |
| direct | 知見カタログを `docs/insights/` に分離 | `insights/` 一式 + README / PR #758 |
| agent-teams-actions | 日次ダイジェスト自動生成 | `daily-digest/2026-08-08.md` |

## 本日の主要な成果

### 1. 日次ダイジェスト 247 件の徹底解析

**GitHub Pages の HTML ではなく MD 原本**を読んだ（HTML は変換物のため）。

**キーワード検索を主手段にしなかった**のが分岐点だった。`Skill` で 4 件・`レビュー` で 4 件しかヒットせず、実際に最も効いた記事群（CLAUDE.md 削減系 6 件）を取りこぼしたため、**212 件のタイトルを全件目視でクラスタリング**する方式に切り替えた。

| クラスタ | 件数 | 関連度 |
|---|---|---|
| CLAUDE.md / 指示の削減 | 6 | **直撃** |
| Skill の運用・弁別性 | 3 | **直撃** |
| レビュー自動化・品質保証・仕様駆動 | 11 | 高 |
| モデル/インフラ動向 | 約 180 | 低 |

関連度「直撃」の 3 件のみ curl で原文精読した。

### 2. 実測で得られた基準値

| 対象 | 実測 | 記事の基準との比較 |
|---|---|---|
| CLAUDE.md + rules | **37,309 字** / 強い言葉 **78 箇所** | 公式は「短く保つ」立場。乖離の可能性 |
| 「検証しろ」型の指示 | **無し** | Opus 5 では削除推奨とされる類は不在 |
| Skill frontmatter | 4,028 字 / トリガー語 **89 語すべてユニーク** | **票の取り合いは起きていない** |
| hooks コメント（shell） | 比率 11.5% / 4 行以上のブロック 15 個 / 最大 29 行 | 記事のルール適用後（8.8% / 18 行）を**上回る** |

### 3. 学習プロセスの Skill 化

記事が提示する**作成ゲート 4 問**（反復性 / アップリフト / 区別可能性 / オーナーと寿命）を通し、4 問すべて通過したため `/company-digest-insights` を新設した。

**トリガー語を 1 件差し替えている。** 当初候補の「ダイジェスト解析」は既存 4 語（`日次ダイジェスト対応` 等）と**部分一致**したため不採用とし、「知見抽出」「ネタ探し」等に変更した。完全一致だけ見ると票の取り合いを見逃す。

Skill には次を明文化した。

- 原本（MD）を読む。HTML は変換物
- キーワード検索を主手段にしない（実績つき）
- 原文は curl で取得
- **主張を鵜呑みにせず自組織で実測する**
- **採用しない判断も理由つきで記録する**

### 4. 知見文書を `docs/insights/` に集約

知見系 5 本（1,202 行）が設計書系 10 本（8,149 行）と `docs/research/` に混在し、3,391 行の設計書に埋もれていた。

```
docs/insights/
├── README.md              ← 入口
├── catalog.md             ← 採用判断カタログ（本体）
└── analyses/              ← 個別の解析レポート（根拠）
```

`git mv` で**ファイル履歴を保ったまま**移動し、ファイル名も短縮した（`ai-driven-development-practices-catalog.md` → `catalog.md`）。

参照の扱いを分けた点が要点で、**現行動作に影響するもの（hooks / rules / skills / plugins）だけ更新し、task-log や過去レポートは当時の記録として据え置いた**。

## 作成・更新された成果物

| ファイル | 内容 |
|---|---|
| `docs/insights/README.md` | 入口（どこから読むか・採用候補・旧新パス対応表） |
| `docs/insights/catalog.md` | 採用判断カタログ（§5.6 追記・優先度表更新） |
| `docs/insights/analyses/2026-08-digest-ai.md` | 8 月ダイジェストの解析（新規） |
| `docs/insights/analyses/comment-density.md` | コメント分量の解析（新規） |
| `.claude/skills/company-digest-insights/SKILL.md` | 知見抽出 Skill（新規・plugins に同期） |
| `.claude/hooks/measure-comment-density.py` | 測定ツール（新規） |
| `CLAUDE.md` | 注意事項に 1 行（不確実さを分量で埋めない） |
| `.claude/rules/web-content-fetch.md` | 参照パスを新構成に更新 |

## Issue 動向

- **#754** インタラクションログ 2026-08-08（オープン）

本日クローズした Issue はなし。

## 次のアクション候補

1. **候補 B / S の統合検討**（根拠: どちらも「動いたが使えるか未検証」を扱う。`verification-pending` の追加と「緑 ≠ 実用可」の明文化は同じ問題の別角度）
2. **候補 Q の判断材料を揃える**（根拠: CLAUDE.md 37,309 字。ただし `@参照` が常時ロードか遅延ロードか**未確定**で、先に挙動確認が要る）
3. **残るクラスタの深掘り**（根拠: 212 件のうち原文精読は 3 件のみ。レビュー自動化 6 件・品質保証 2 件は**タイトルからの推定に留まる**。`/company-digest-insights` で継続できる）
4. **NFR-05 の目標値判断**（根拠: 67.09% で未達。設計時の到達可能性見積もり 78.6% にも届いていない）
5. **案 2（エージェント実行トレースビューア）の要件定義**（根拠: `proj-ai-virtual-office` の中核。設計フェーズが空いている）

## 未検証事項

- **`@参照` された rules が常時ロードか遅延ロードか**（候補 Q の判断に直結。Claude Code の内部実装は非公開）
- 本リポジトリの ⚠️ 記号が記事の言う `CRITICAL:` と同じ overtrigger を招くか
- **212 件のうち原文を読んだのは 3 件のみ**。他クラスタはタイトルからの推定
- 新設した Skill / ルールが実際に効くかは**次回以降の観察待ち**
- 過去の PR / Issue に残る旧パスは**リンク切れになる**（履歴として許容する方針）

---
_このレポートは /company-report Skill によって自動生成されました_
_情報源: .session-summaries/ | .task-log/ | docs/ | GitHub Issues | .conversation-log/_
