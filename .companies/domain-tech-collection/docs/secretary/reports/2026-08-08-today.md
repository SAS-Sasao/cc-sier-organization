# domain-tech-collection 活動レポート — 2026-08-08（土）

> 生成日時: 2026-08-08 11:00 JST | 生成者: SAS-Sasao | 期間: 2026-08-08（当日）

## エグゼクティブサマリー

**外部知見を自組織で実測し、その結果に基づいてルールを 1 件追加した日**。

Claude が書くコメントの冗長さを扱った記事を解析し、記事の教訓「体感で判断せず測る」に従って本組織の hooks を実測した。結果、**記事のルール適用後の水準を上回る冗長さ**（比率 11.5% / 4 行以上のブロック 15 個 / 最大 29 行）が確認され、さらに「置き場所」基準で **CLAUDE.md と hooks コメントに計 11 箇所の知見重複**が見つかった。

精度向上への反映として、記事自身が「ルールは半分しか効かない」と示していることを踏まえ、**最も効く 1 件のみ**を CLAUDE.md に追加した（不確実さを分量で埋めない）。あわせて測定を再現可能にするツールを作成している。

なお前回 cycle（8/3）から 5 日空いたが、**8/4〜8/7 は日次ダイジェストの自動生成のみ**で、対話的な作業は発生していない。

## 活動統計

| 指標 | 値 |
|------|-----|
| セッション数 | 1 回 |
| ツール実行数 | 16 回 |
| 完了タスク数 | 2 件 |
| マージ PR 数 | 1 件 |
| 新規 Issue 数 | 1 件 |
| 作成・編集ファイル数 | 4 件 |

ツール内訳: bash 11 / write 5

## 完了タスク

| モード | タスク | 成果物 |
|---|---|---|
| direct | AI が書くコメントの分量 解析 + 自組織での実測 | `comment-density-analysis.md` / カタログ §5.5 / CLAUDE.md 1 行 / 測定ツール / PR #753 |
| agent-teams-actions | 日次ダイジェスト自動生成 | `daily-digest/2026-08-08.md` |

## 作成・更新された成果物

| ファイル | 内容 |
|---|---|
| `docs/research/comment-density-analysis.md` | 記事解析 + 自組織での実測（新規） |
| `docs/research/ai-driven-development-practices-catalog.md` | §5.5 追記・採用候補 N/O/P を追加 |
| `CLAUDE.md` | 注意事項に 1 行（不確実さを分量で埋めない） |
| `.claude/hooks/measure-comment-density.py` | コメント密度の測定ツール（新規） |

## 本日の技術的成果

### 1. 記事の主張を自組織で実測した

記事の核心は「**コメントの分量が、その箇所の不確実さの指標になっている**」という指摘。著者が Claude 本人に聞いた結果、「自信がない箇所ほど言葉で埋めて補強しようとする」という回答が得られている。

さらに著者は、ルールを入れても**長いブロックが 52 → 50 個でほぼ減らなかった**ことを実測で示した。理由は「無意味だから消せるのではなく、**有用だから消せない**」。

本組織で同じ指標を測定した結果:

| 対象 | 比率 | 4 行以上のブロック | 最大 |
|---|---|---|---|
| `.claude/hooks/*.sh` | **11.5%** | **15 個** | **29 行** |
| `.claude/hooks/*.py` | 6.3% | 2 個 | 7 行 |

記事のルール適用後（8.8% / 最大 18 行）を上回っている。

### 2. 「置き場所」基準で知見の重複を発見

記事の第 2 の基準（そこがその情報の置き場所か）を当てたところ、**同じ運用知見が CLAUDE.md と hooks コメントの両方に存在**していた。

| 知見 | CLAUDE.md | hooks コメント |
|---|---|---|
| `PROJECTS_PAT` は Classic PAT 必須 | 1 箇所 | **10 箇所** |
| fine-grained PAT の制約 | 1 箇所 | 4 箇所 |
| Python→bash は `\x1f` を使う | 1 箇所 | 3 箇所 |

CLAUDE.md は常時ロードされ、hooks コメントはそのファイルを読んだときにしか目に入らない。**同じ制約を 2 箇所で保守する状態**になっている。

### 3. ルール追加は 1 件に絞った

記事自身が「ルールは半分しか効かなかった」と実測で示しているため、複数のルールを一度に足すことは避けた。追加したのは次の 1 行のみ。

> 不確実さを分量で埋めない。「未確認」と明示的に書く。

既に 4 レポートで「未検証事項」章を設ける運用が定着していたが、**ルール化されておらず判断に依存**していた。慣行の明文化にあたる。記事の推奨に従い禁止リスト形式で置いた。

### 4. 測定を再現可能にした

`.claude/hooks/measure-comment-density.py` を作成。記事の教訓「ルールを書いた後は、出力が実際に変わったかを測る」を次回実行できるようにした。レポートの数値と完全一致することを実行確認済み。

## Issue 動向

- **#754** インタラクションログ 2026-08-08（オープン）

本日クローズした Issue はなし。

## 前回 cycle からの空白期間について

前回 cycle は 8/3。8/4〜8/7 の 4 日間は**日次ダイジェストの自動生成のみ**で、対話的な作業は発生していない。

| 日付 | 活動 |
|---|---|
| 8/4〜8/7 | 日次ダイジェスト自動生成（PR #743 / #745 / #747 / #749 / #751）+ ダッシュボード・TodoInsights の自動更新 |

この期間の task-log は `daily-digest-actions` のみで、レポート対象となる判断や成果物は無い。

## 次のアクション候補

1. **候補 B の実装検討**（根拠: カタログで優先度「高」。task-log に `verification-pending` を追加。`retail-stats-tracker` で「動いたが使えるか未検証」が `completed` として記録される事態が継続中）
2. **NFR-05 の目標値判断**（根拠: 8/3 時点で 67.09%、目標 80% に未達。設計時の到達可能性見積もり 78.6% にも届いていない）
3. **候補 O の検討**（根拠: hooks と CLAUDE.md の知見重複 11 箇所。ただし `/company-spawn` で新リポにコピーされる場合の参照切れをどう扱うかの判断が要る）
4. **追加したルールの効果測定**（根拠: 候補 P。記事の著者も「体感では効いた/効かないを取り違える」と明言。測定ツールは用意済み）
5. **案 2（エージェント実行トレースビューア）の要件定義**（根拠: 7/25 の壁打ちで本命と位置づけた `proj-ai-virtual-office` の中核。設計フェーズが空いている）

## 未検証事項

- 本日追加したルールが実際に出力へ効くかは**次回以降の観察待ち**
- 設計書・task-log の冗長性は**未測定**（今回は hooks のみ）
- 「コメント量 = 不確実さの指標」が本組織の成果物にも当てはまるかは**仮説の段階**

---
_このレポートは /company-report Skill によって自動生成されました_
_情報源: .session-summaries/ | .task-log/ | docs/ | GitHub Issues | .conversation-log/_
