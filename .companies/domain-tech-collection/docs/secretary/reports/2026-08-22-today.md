# domain-tech-collection 活動レポート — 2026-08-22（土）

> 生成日時: 2026-08-22 16:40 JST | 生成者: SAS-Sasao | 期間: 2026-08-22（today）

## エグゼクティブサマリー

**外部記事 1 本の解析（ループエンジニアリングと Proof-or-Stop）**を軸に、その過程で **6 日間気づかれていなかった不具合を 1 件発見・修正**した。オーナーの「日次ダイジェストの追加処理で解析カード消している？」という指摘が起点である。

本日の解析で得た最重要の概念が **Proof-or-Stop**（「AI の『終わりました』は事実ではなく、ただの主張である」）だが、**同じ日に自組織でその実例が 2 件出た**。①`git fetch` 前のローカルを見て「ダイジェストは 8/16 が最新」と報告し直後に訂正、②8/16 に「解析カードは消えない」と確認したが片方の経路しか見ておらず、実際は毎晩消えていた。

**記事の 5 ステップに照らすと、本組織は既に外側のループ 2 本まで到達している。** 弱点は 2 点に絞られた（レビュアーが同じ Claude / 停止条件が 5-18 Skill）。

未完了の懸念は、採用候補が **A〜BF の 58 項目**に膨らみ、**優先度「高」だけで 6 件**たまっていること。1 件も着手していない。

## 活動統計

| 指標 | 値 |
|------|-----|
| セッション数（`.session-summaries`） | 3 回 |
| ツール実行数（会話ログ実測） | **556 回** |
| 人間の発言数 | **319** |
| 作成・編集ファイル数 | 18 件 |
| 完了タスク数 | 2 件 |
| main コミット数 | 6 件 |
| マージ済み PR | 1 件（#805） |

> **注記**: `.session-summaries` 側は「3 セッション / ツール実行 60」を返すが、会話ログ側は 556 である。**本セッションが長時間継続中でセッション境界のサマリーがまだ書き出されていない**ためと考えられる。**未調査**。本レポートは会話ログ側の実測を採用した。
>
> なお 8/16 のレポートでも `.session-summaries` の `files_written` が全件 0 という別の異常を記録しており、**同ファイル群の信頼性は継続的な懸念**である。

## 完了タスク

- **[agent-teams-actions]** 日次ダイジェスト 2026-08-22（GitHub Actions 経由）
- **[direct]** `/company-insights-cycle` — sbbit 記事 3 ページの解析（ループエンジニアリング / Proof-or-Stop）→ PR #805

## 進行中タスク

なし。

## 作成・更新された成果物

### 知見解析

- `docs/insights/analyses/loop-engineering-proof-or-stop.md`（新規）
- `catalog.md` §5.20（ループエンジニアリング）/ §5.21（カード消失）+ 候補 **BC〜BF**
- `README.md` 統計を実測へ（**129 日分** / 解析済み 11.6%）、採番範囲 A〜BF

### 不具合修正

- `.claude/hooks/generate-insights-html.py` — 削除正規表現を完全一致へ

## Issue 動向

### マージされた PR（期間内）

- **#805** ループエンジニアリングと Proof-or-Stop を解析 + 解析カード消失を修正

### 新規オープン / クローズされた Issue

なし。

## 会話トピック

### セッション別の依頼内容

- **セッション 9637b4fe**（319 発言 / 550 応答 / 556 ツール実行）
  - `claude` 起動時の `apt_pkg` エラーの原因調査
  - `/company` 起動（組織状態の確認）
  - sbbit 記事 3 ページの解析（Skill 経由での実行を明示指定）
  - **「日次ダイジェストの追加処理で解析カード消している？」の指摘**
  - `/company-cycle` の実行

### ファイル生成を伴わなかったやり取り

- `claude` コマンドが起動しない件の原因切り分け（**npm インストール中断で `package.json` が入らず postinstall が失敗** → 再インストールで解消）
- `apt_pkg` エラーは Python 3.10 → 3.12 の更新に `.so` が追随していないため。**claude とは無関係のノイズ**であることを説明（対処は sudo が必要なため未実施）

## 本日の発見 — 6 日間気づかれなかった不具合

### 事象

`docs/index.html` の「知見解析レポート」カードが **0 件**になっていた（2026-08-16 の公開時は 1 件）。

### 原因

`generate-insights-html.py` の削除正規表現。

```python
r'\s*<a href="\./insights/[^"]*"[^>]*class="card"[^>]*>.*?</a>'
```

`./insights/[^"]*` が **`./insights/analyses/index.html` にもマッチ**する。同 hook は `daily-insights-sync.yml` から**毎晩実行**されるため、解析カードが毎晩巻き添えで削除され TodoInsights だけが再挿入されていた。

### なぜ 8/16 の確認をすり抜けたか

`docs/index.html` を書き換える処理は **4 つ**ある。

| 処理 | 起動元 |
|---|---|
| `generate-insights-html.py` | **daily-insights-sync（毎晩）** ← 犯人 |
| `generate-daily-digest-html.sh` | **daily-digest-automation（毎晩）** |
| `generate-insights-analyses-html.sh` | ローカル `/company-insights-cycle` のみ |
| `generate-dashboard.sh` | **どの workflow からも呼ばれない**（ローカル `/company-dashboard` 専用） |

8/16 に「`generate-dashboard.sh` にもカード定義を置いたので消えない」と確認したが、**そもそも nightly から呼ばれていない経路**だった。片方しか見ずに「消えない」と報告していた。

### 対処

正規表現を `\./insights/index\.html` の完全一致に変更。**夜間 2 処理を模擬実行して、解析カード・TodoInsights・日次ダイジェストの 3 枚とも生存することを実測**した。

**未確認**: 実際の nightly での生存は**次の夜まで分からない**。

## 次のアクション候補

1. **候補 BC — Proof-or-Stop の明文化**（根拠: `completed` 237 件中 **92.4%** が未検証事項を書いていない。本日も「消えない」と報告して証拠を取っていなかった実例が出た。既存の B / C / S / AL を 1 本に束ねられる）
2. **候補 AO + AD — CLAUDE.md を削るのをやめて `AGENTS.md` の畳み込みへ**（根拠: 1,650 回実測で長さは想起に効かないと判明済み。`AGENTS.md` は 2026-06-13 から未更新）
3. **明日のトップページ確認**（根拠: 本日の regex 修正が nightly で効くかは未検証。**消えていたら `generate-daily-digest-html.sh` 側にも同種の取りこぼしを疑う**）
4. **候補 AE の残り — `plugins/` に無い Skill 3 種の移送**（根拠: VCS 真ソース原則の外にある）
5. **`.session-summaries` の信頼性調査**（根拠: 本日はツール実行数が会話ログの 1/9、8/16 は `files_written` が全件 0。**2 回続けて異常値**）

## 本レポートで確認していないこと

- `.session-summaries` と会話ログの**数値乖離（60 vs 556）の原因を調べていない**
- 本日の regex 修正が**実際の nightly で効くかは次の夜まで未確認**（模擬実行のみ）
- `generate-dashboard.sh` が workflow から呼ばれていないのが**設計意図か放置かを確認していない**
- 記事の参照元（LoopsBench / Proof-or-Stop 論文 / Anthropic 公式ブログ）の**原典をいずれも読んでいない**
- 採用候補は **A〜BF の 58 項目**。**優先度「高」6 件を含め未着手**

---
_このレポートは /company-report Skill によって自動生成されました_
_情報源: .session-summaries/ | .task-log/ | docs/ | GitHub Issues | .conversation-log/_
