---
task_id: "20260803-104500-web-fetch-rule"
org: "domain-tech-collection"
operator: "SAS-Sasao"
status: completed
mode: "direct"
started: "2026-08-03T10:45:00"
completed: "2026-08-03T10:55:00"
request: "取得方法については、私が直接解析依頼する際のルールにしてほしい。SKILLなどでルール化してほしい。"
issue_number: null
pr_number: null
subagents: []
l0_gate: null
l0_retries: 0
l1_gate: null
l1_retries: 0
l2_composite: null
l2_retries: 0
---

## 実行計画

- **実行モード**: direct（ルール文書の作成。委譲の必要なし）

## 配置先の判断: SKILL ではなく rules + CLAUDE.md

依頼は「SKILL などでルール化」だったが、**SKILL.md ではなく `.claude/rules/` + `CLAUDE.md` に配置**した。

理由: **SKILL.md は skill 発動時にのみロードされる**。今回ルール化する対象は
「オーナーが URL を渡して解析を依頼する」という**skill を経由しない直接依頼**であり、
SKILL.md に書いても発火しない。

常時ロードされるのは `CLAUDE.md`（セッション開始時）のみ。
`rules/` は `CLAUDE.md` からの @参照で読み込まれる。この経路でなければ実効性がない。

## 実施内容

### 1. `.claude/rules/web-content-fetch.md` を新設

構成:
- 原則（解析依頼では curl で原文取得）と、要約経路で失われるもの 3 点
- 判断基準の表（記事プラットフォーム = curl 必須 / 公式ドキュメント = WebFetch 可 / 不明 = curl）
- 実行手順（curl コマンド + Python での本文抽出）
- 失敗時の対処 4 パターン
- 報告時の注記（取得方法と文字数を明示）
- 適用範囲（適用する / しない の区別）

### 2. `CLAUDE.md` に 2 箇所追加

- **注意事項に 1 行**（⚠️ 付き）— 実効性を高めるため。外部参照だけでは見落とされる
- **外部参照テーブルに 1 行** — 詳細への導線

## 記載した知見のうち重要なもの

### 信頼ドメインの完全なリストは未確認であることを明記

記事は「80 以上」とするが、本組織で実測確認したのは `docs.aws.amazon.com` のみ。
**`aws.amazon.com`（ブログ・What's New）は対象外**である点も明記した。
不確かなことを確定事項として書かない。

### バイパス経路では prompt が効かない副作用

信頼ドメインで WebFetch を使うと、「H2 の見出しのみ」と指示しても全文が返る。
検証で発見した挙動であり、要約が欲しい場合は取得後に自分で絞る必要がある。

### script/style を先に除去する

タグ除去を先にすると JS のコードが本文に混入する。手順の順序として明記した。

### curl 不可時のフォールバック規定

SPA・認証・タイムアウト時は WebFetch にフォールバックしてよいが、
**「要約経由である旨を報告に明示する」ことを義務づけた**。
黙って要約を原文として扱わないことが要点。

## 検証

ルールに記載した curl コマンドと Python 抽出手順を、
検証で使ったのとは別のサイト（`zenn.dev/notahotel/...`）に対して実行し、
1,871 文字の本文抽出に成功。script 混入がないことも確認した。

**手順書に書いたコマンドが実際に動くことを確認してから commit している。**

## 成果物

- `.claude/rules/web-content-fetch.md`（新規）
- `CLAUDE.md`（注意事項 + 外部参照に各 1 行）

## reward
（post-merge hook が自動追記）
