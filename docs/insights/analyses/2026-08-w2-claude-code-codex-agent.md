# 2026-08 第2週 AI関連ダイジェスト解析 — Claude Code / Codex / エージェント設計

| 項目 | 内容 |
|------|------|
| ドキュメント種別 | 解析レポート（原文精読 + 自組織実測） |
| 作成日 | 2026-08-16 |
| 作成者 | 技術リサーチ室（秘書室主導） |
| 依頼 | 「8/9週で AI 関連の配信のうち、私が使用できそうな物（Claude Code, Codex, AI エージェント設計関連）を深堀して解析」 |
| 対象期間 | **2026-08-03（月）〜 2026-08-15（土）** の日次ダイジェスト A1 / A2 章（§9 で 08-10〜08-15 を追補） |
| 母数 | 記事 402 件 / ユニークタイトル 349 件（本編 181 + 追補 168） |
| 精読 | **20 件**（すべて `curl` で原文取得。本編 12 + 追補 8） |
| ステータス | 解析完了（2026-08-16 追補・訂正済み） |

---

## 0. 方法

### 0.1 対象期間の確定

依頼の「8/9週」を **2026-08-03（月）〜 08-09（日）** と解釈した。根拠は次の 2 点。

1. 2026-08-09 は日曜日であり、月曜起点の週は 08-03 から始まる
2. ~~日次ダイジェストは 2026-08-09 が最新で、08-10 以降は未生成~~ ← **この根拠は誤りだった（2026-08-16 訂正）**

> **【訂正】2026-08-16**
> 上記 2 は事実ではない。解析時にローカルリポジトリを `git fetch` しておらず、**古いチェックアウトを実査して「08-10 以降は未生成」と判断した**。実際には 2026-08-10 〜 08-16 の 7 日分がリモートに存在していた（08-10: 38 件 / 08-11: 35 件 / 08-12: 33 件 / 08-13: 24 件 / 08-14: 30 件 / 08-15: 36 件 / 08-16: 39 件）。
>
> したがって「8/9 起点の週（08-09〜08-15）」と解釈した場合、**本レポートがカバーしているのは 08-09 の 1 日分（20 件）のみ**で、残り 6 日分・196 件は読んでいない。根拠 1（08-09 が日曜）は成立するため 08-03〜08-09 を解析したこと自体は有効だが、**「どちらの解釈でも取りこぼしがない」という主張は誤り**である。
>
> 皮肉なことに `/company-digest-insights` の Phase 0 には「**git fetch してリモート差分を確認（ローカルが古いと最新ダイジェストを取りこぼす）**」と明記されている。本レポートは `/company` 経由で実行したためこの手順を踏まなかった。

「8/9 から始まる週（08-09〜08-15）」と解釈した場合でも、データが存在するのは 08-09 の 1 日分だけである。08-03 起点で取ることは、どちらの解釈に対しても取りこぼしがない。

### 0.2 取得方法

ダイジェスト MD 原本（`.companies/domain-tech-collection/docs/daily-digest/2026-08-0{3..9}.md`）を直接読んだ。GitHub Pages の HTML は MD からの変換物であり、変換前を読む方が欠損の可能性がない。

精読対象の外部記事 12 件はすべて `curl`（User-Agent 付き）で原文 HTML を取得し、`script` / `style` を除去してテキスト化した。**WebFetch は使っていない**（`@.claude/rules/web-content-fetch.md` に従う。Zenn / Qiita / はてブ / 企業技術ブログはいずれも信頼ドメイン外）。

### 0.3 絞り込み

181 件のユニークタイトルを全件目視し、依頼の 3 レンズで分類した。

| レンズ | 定義 |
|---|---|
| **① Claude Code 実運用** | 今日から設定・運用を変えられるもの |
| **② Codex 併用** | Claude Code と Codex を同時に使う際の問題 |
| **③ AI エージェント設計** | ハーネス・ツール・ゲートの設計思想 |

既存解析（`2026-08-digest-ai.md` / `ai-pr-approval-gate.md` / `comment-density.md` / `task-management.md`）で原文精読済みの記事は、重複を避けて精読対象から外した。具体的には次の 5 件。

- CLAUDE.md を厚くしても意味がなかった話
- Claude Code の Skills は「手順書」だった
- 58% の Pull Request を AI が承認するようになった
- Claude が書く長いコメントは、Claude 自身の役に立っていなかった
- Claude Code／Codex に中〜大規模開発を任せるためのタスク管理

---

## 1. 母集団の内訳（181 件）

依頼の 3 レンズに対する関連度でクラスタ化した。

| # | クラスタ | 件数 | レンズ | 精読 |
|---|---|---|---|---|
| A | Claude Code の運用テクニック（トークン・並列・plan・権限） | 14 | ① | 4 |
| B | Claude Code バージョンアップデート（v2.1.220〜226） | 5 | ① | 0 |
| C | Claude Code の組織展開・リモート実行 | 6 | ① | 1 |
| D | Codex 併用・設定同期・コスト制御 | 4 | ② | 2 |
| E | エージェントハーネスの設計 | 5 | ③ | 1 |
| F | エージェント向けツール設計（MCP / CLI / Agent Plugins） | 8 | ③ | 1 |
| G | AI レビュー・自動マージ・承認ゲート | 9 | ③ | 2 |
| H | エージェント運用の安全・権限・リスク | 6 | ③ | 1 |
| I | ナレッジ表現（オントロジー / LLM-Wiki / OKF / RAG） | 11 | — | 0 |
| — | モデル・基盤動向（Bedrock / GPT-5.6 / Kimi K3 / ハード等） | 約 113 | 対象外 | 0 |

**モデル・基盤動向が母集団の約 6 割**を占める。これらは「読めば面白いが、笹尾さんの手元の設定を変える材料にはならない」ため、今回は採点対象から外した。クラスタ I（ナレッジ表現）は関連はあるが、3 レンズのいずれにも直接は乗らないため精読を見送っている。

---

## 2. レンズ① Claude Code 実運用

### 2.1 サブエージェントは「1 件ずつ」より「まとめて並列」が速く、かつ安い

**出典**: [検証！Claude Code のサブエージェント並列はお得なのか](https://qiita.com/tamashiro_nobuyuki/items/ff9004b66b7761c4d34c)（tamashiro_nobuyuki / 株式会社NEXT TECHNOLOGY、2026-07-31、curl 3,621 字）

同一の独立 3 タスクを 3 通りの方法で実行し、所要時間・トークン・メイン文脈の増加量を計測した記事。

| 群 | 所要時間 | 全体トークン | メイン文脈の増加 |
|---|---|---|---|
| ① 直列・サブエージェント無し | 約 9.5 秒 | 40,770 | 5,876 |
| ② 直列・サブエージェント経由 | 約 30.0 秒 | 119,194 | 6,183 |
| ③ **並列**・サブエージェント経由 | 約 14.3 秒 | **66,884** | **3,473** |

意外なのは **②→③ でトークンが 44% 減っている**点。著者の分析では、同時に似た内容のサブエージェントを起動したことで**定義部分のキャッシュを共有でき**、③ では新規キャッシュ書き込みがほぼ発生せず、コストが約 1/10 のキャッシュ読み込みが大半を占めたため。

一方、著者が期待していた「メイン文脈の保護」効果は**確認できなかった**と正直に書いている。理由は、依頼したタスクが `tool_uses: 0` の一発回答で、そもそもサブエージェント側に隠すべき試行錯誤が無かったから。

**この記事の限界**（著者自身が明記）: 測定は 3 タスク・1 セットのみ。査読も反復もない。「重いタスクでの文脈保護効果は未検証」と結んでいる。

### 2.2 `isolation: worktree` — 「決定論的な処理を LLM の外へ」だけでは足りない

**出典**: [【並列開発】isolation: worktree やめました 〜決定論と認知を分けて AI と仕事する〜](https://zenn.dev/rakko_inc/articles/2da36c0abe4dbe)（marimo / ラッコ株式会社、2026-08-06、curl 6,973 字）

Claude Code の `isolation: worktree`（サブエージェントを専用 git worktree で動かす設定）を並列開発に使っていたが、やめた記録。

著者は「起動のたびに壊れ方が変わる」現象を追い、**原因が呼び出し方にあった**ことを突き止めている。

> Claude Code のサブエージェントには起動のしかたが 2 通りあり、`isolation: worktree` はそのうち片方でしか効きません。
> - **名前を付けずに起動する** … 設定通り専用の worktree が用意される
> - **名前を付けて起動する** … メインの作業ディレクトリをそのまま使う。`isolation: worktree` の指定は**無視される**

さらに、本体はエージェントを起動時のパスに固定するため、そのパスを自分で `git worktree move` すると足場が消える。

注目すべきは、**原因が分かった上で「直さずにやめた」**点。worktree の用意は入力が決まれば出力が決まる決定論的な処理であり、そもそも Claude Code の機能に載せる必要がなかった、という判断である。シェルスクリプト 1 本（`setup-worktree.sh`）に置き換えた結果、`isolation` 指定・Hooks・改名工程・「名前付きで起動してはいけない」という運用上の注意が、まとめて消えた。

教訓として提示されているのが、次の物差し。

> 見分ける物差しとして使えるのが、**単体で叩いて確かめられるか**です。

そして、この不安定さは「AI の気まぐれ」に見えるが実際には配線の問題であり、**プロンプトを直そうとしてしまう**のが厄介だ、と書いている。

> LLM の判断から外しても、呼び出し方しだいで挙動が変わる仕組みに載せていれば同じことです。

なお本記事は 2026 年 7 月時点の挙動であり、著者もバージョンによって変わりうる点を注記している。

### 2.3 プロンプトキャッシュ — 「再開一発目」のコストは順番で決まる

**出典**: [プロンプトキャッシュを意識して claude code のトークンを節約しよう](https://zenn.dev/ceres_tech_blog/articles/7163ae482e124e)（tamapon / 株式会社セレス、2026-08-04、curl 5,931 字）

「前日のやり取りを複数再開したら、まだ一言も打っていないのに 5 時間制限が 2 割持っていかれた」という実体験から書かれた記事。公式ドキュメントの参照が多く、記事内で**どこが公式記載でどこが自分の推論か**を明示している点が信頼できる。

**キャッシュが飛ぶ条件**（記事の列挙）:

| 条件 | 補足 |
|---|---|
| 1 時間触らない | 日跨ぎは確定でこれ |
| `/model` によるモデル切り替え | キャッシュはモデル単位。opusplan の plan mode 出入りも該当 |
| `/effort` による effort 変更 | キャッシュキーはモデルと effort の組 |
| 自動モデルフォールバック | safety classifier に引っかかった場合 |
| MCP サーバの接続/切断 | ただし tool search 対応モデルでは遅延ロードのため壊れない |
| ツールまるごと deny | `Bash` は飛ぶ。`Bash(rm *)` のようなスコープ付きは飛ばない |
| Claude Code のアップグレード | 次回起動でシステムプロンプトが変わる |

**対策の中核は「順番」**である。

> `/compact` は会話履歴を短い要約に置き換える。この要約を作る処理はキャッシュがまだ生きてるうちに走るから安く済む。（中略）逆に「再開してから /compact」だと、キャッシュが切れた履歴を全部読んでから要約することになるので、再処理ぶんは丸々払う。**順番が全て。**

すでに切れてしまった場合は、`/model` で Sonnet 等に落としてから `/compact` し、元に戻す。全履歴の再処理 1 回は避けられないが、**どのモデルに払わせるかは選べる**、という理屈。

著者が「公式ドキュメントに明記がなく、記述からの帰結として書いた」と断っているのは 2 点（要約が選択中のモデルで走ること / 5 時間・週次ウィンドウ側の節約分）。**この 2 点は本レポートでも未検証扱いとする。**

### 2.4 cclens — 設定の棚卸しを「計測」に置き換える

**出典**: [Claude Code の「無駄」を可視化するツール cclens を作った](https://zenn.dev/lambdalisue/articles/introduce-cclens)（Alisue、2026-08-05、curl 8,824 字）

問題設定が、本組織の状況とそのまま重なる。

> ある目的のためにルールやスキルを書いたとして、それが本当に効いてるのか？（中略）気付いたら使われない設定だけが静かに積もっていく
> じゃあ棚卸しするか、となっても、今度はその非決定性が逆に効いてきます。「もしかしたら自分の知らないところで効いてるのかも」と思うと、途端に消せなくなる。

cclens は**計測用に何かを走らせるのではなく、普段使いのログ（`~/.claude/projects/**/*.jsonl`）をそのまま解析する**。Rust 製・全ローカル完結・データ送信なし。

| コマンド | 見えるもの |
|---|---|
| `cclens doctor` | 1 画面の健康診断（global / project 別） |
| `cclens waste` | 最適化余地のランキング（削除 / スリム化 / トリムの推奨付き） |
| `cclens inventory` | 全設定サーフェス × 実際の使用状況 |
| `cclens overhead` | **毎セッション必ず払う常時オンのコンテキスト量**（自分の設定由来 / 削れない下駄 に分解） |
| `cclens usage` | スキルごとの使用回数ランキング |
| `cclens failures` | ツール失敗のカテゴリ別集計 |
| `cclens stuck` | Claude が短時間に何度も編集し直したファイル |
| `cclens optimize` | findings を Claude 自身に渡して修正案を出させる |

本組織にとって効くのは `overhead` と `usage` と `inventory` の 3 つ。理由は §5 の実測を参照。

### 2.5 plan モードは要らなくなった（という主張）

**出典**: [【Claude Code】plan モードはもう使っていない](https://zenn.dev/haru067/articles/0c28638945aa32)（haru067 / NOT A HOTEL、2026-07-29、curl 1,974 字）

短い記事だが論旨は明快。plan モードの役割は「判断を求める停止点」と「Edit 禁止のガードレール」の 2 つで、どちらも**「案をください」と一言言うだけで解決する**、という主張。

plan モードより優れている点として挙げているのが Human-in-the-Loop。

> plan モードの典型的な敗北は、人間が知らない調査のコンテキストを使った結論を列挙し、人間の認知負荷を超えた読むに堪えない実装案の羅列を吐き出してしまうことだ。そして人間は読むに耐えないので脳死で許可し、あとで検証すればいいか、となる。

「案をください」なら一問一答ではなく何往復もできる、というのが利点だという。

**この主張の扱いには注意が要る。** 記事は Anthropic のブログ（context engineering）を根拠に「今のモデルは十分に賢いので雑な命令でよい」としているが、**cc-sier は Skill が固定フェーズを持つ設計**であり、「雑な命令で全部良い感じに」とは前提が異なる。§6 では優先度を低く置いた。

---

## 3. レンズ② Codex 併用

### 3.1 CLAUDE.md / AGENTS.md / Skills の二重管理をどう畳むか

**出典**: [【決着】Claude Code と Codex の設定ファイルを同期させる（みんな仲良く）](https://zenn.dev/kanta_tkd/articles/20f7f41cff8428)（Kanta / 株式会社エクスプラザ、2026-07-28、curl 3,413 字）

課題の記述が、そのまま本組織に当てはまる。

> 同じプロジェクトで Codex と Claude Code の両方を使っているとき、AGENTS.md や SKILLS などの変更が片方にしか適用されない
> 何も意識せずに両方を使っていると、片方でのみ更新が入ってそのまま・・・ という状況が起きますし、なんならそれぞれ独自に分岐していく未来が容易に想像できますよね。

著者の解法は **「形式が同じものはコピーを作らず symlink」**。

| 対象 | 手段 |
|---|---|
| AGENTS.md / CLAUDE.md | **CLAUDE.md に `@AGENTS.md` と記載**して AGENTS.md を読ませる |
| Skills | `.claude/skills/` を `.agents/skills/` へ symlink（git は symlink をリンク先パス文字列として記録するのでそのまま版管理できる） |
| MCP（`.mcp.json` JSON ↔ `.codex/config.toml` TOML） | 書式が違うため変換ロジック。中立形に正規化して比較し、差があれば相手側の書式へ変換 |

> 最終的に、編集されるのは常に Claude 側の 1 か所だけになり、生成・コピーを忘れるという事故が構造的に起きなくなります。

重要なのは **「すべてを一律で同期しない」** という設計判断。

- **Prompting Guide の違い**: Anthropic と OpenAI で推奨される指示の書き方が異なる。Claude 向けに調整した Skill の文面が Codex でも最適とは限らない
- **片方に仕組み自体が無い**: Codex には Claude Code の `rules`（パス条件つきで読み込まれる指示ファイル）に当たる機能が無い。`~/.codex/rules/` は存在するがコマンド承認ポリシーであり別物

そのため著者は、rules については**ファイル一覧と読むべきタイミングだけを AGENTS.md に書き出し**、実際に読むかは読み手のツールに委ねている。

成果物は `agents-sync` CLI + スキル + fswatch/launchd 常駐スクリプト（MIT）。なお著者は「Claude Code CLI から Codex を操作する手法（`claudex` 等）もあり、CLI しか使わない人はそちらが良い」と冒頭で断っている。

### 3.2 Codex の利用量と Budget を Gateway 層で止める

**出典**: [Amazon Bedrock AgentCore Gateway で Codex の利用量と Budget を制御する](https://zenn.dev/kashiwabaray/articles/796539362435eb)（Yutaka Kashiwabara / NTT DATA、2026-08-07、curl 12,971 字）

AWS Samples の `sample-agentcore-gateway-usage-interceptor` を EC2 に構築し、Codex CLI から実際に叩いた検証記録。手順とコマンドが全部載っており、再現性が高い。

構成は、AgentCore Gateway の **Interceptor** をコストガバナンスのポイントとして使うもの。

- **REQUEST Interceptor**: JWT からユーザーを特定 → DynamoDB から現在利用額を取得 → Daily / Monthly Budget を確認 → 超過なら **モデル呼び出し前に HTTP 403**
- **RESPONSE Interceptor**: レスポンスの `usage` からトークン数を取得 → コスト算出 → CloudWatch EMF 出力 → DynamoDB へ加算

検証結果（記事の表）:

| 検証内容 | 結果 |
|---|---|
| Codex → AgentCore Gateway → GPT-5.4 | 成功 |
| DynamoDB へのユーザー別 spend 記録 | 成功 |
| Daily Budget 超過 | HTTP 403 |
| **Block 後の spend 増加** | **なし** |
| Monthly Budget 超過 | HTTP 403 |

著者は**確認できていないことを明示している**点が誠実で、そのまま引用に値する。

> 今回確認したのは **すでに現在利用額が cap を超えているユーザーの次のリクエストを止められること** です。
> （中略）「Budget を超えたユーザーの次の呼び出しを止める」ことと、「設定した Budget を絶対に超過しない」ことは同じなのか という点は、もう少し検証する余地があります。

並列実行時に複数の REQUEST Interceptor が同じ現在利用額を参照して同時通過しうる、という懸念も自分で挙げている。

**本組織への適用**: 現状 1 人運用のため、ユーザー単位のコスト帰属は過剰。**チーム展開（要件定義書 §1.3 Phase 2）に入る段階で効いてくる**設計として記録する。

---

## 4. レンズ③ AI エージェント設計

### 4.1 エージェントハーネスの解剖 — ループ本体は最後まで変わらない

**出典**: [Claude Code のようなエージェントをゼロから作る](https://zenn.dev/sun_asterisk/articles/f5514175aa0a0a)（グエン・タイ・ゴック / Sun*、2026-08-06、curl 18,827 字）

GitHub 70k スターの教材 `shareAI-lab/learn-claude-code`（MIT）全 20 章を読んだ記録。各章が前章のコードに仕組みを 1 つだけ足す構成で、**Claude Code の各機能が「なぜ必要か」を問題起点で説明している**。

第 1 章のエージェントループがすべての土台で、`stop_reason == "tool_use"` なら実行して結果を返し、そうでなければ抜ける、というだけ。

結論が最も価値がある。

> 20 章ぶんの仕組みは、どれもこのループのまわりに「モデルに何を見せて、何をさせるか」を足しているだけで、**ループ本体は最後まで変わりません**。
> エージェントの賢さはモデルが持っていて、ハーネスの仕事はモデルにコンテキストと道具を渡すことだ

cc-sier の理解に直接効く章を抜き出す。

| 章 | 仕組み | cc-sier での対応物 |
|---|---|---|
| 3 | Permission | `settings.local.json` の allow リスト |
| 4 | Hooks（PreToolUse / PostToolUse / Stop） | `.claude/hooks/` 15 本 |
| 6 | Subagent（**親と別の messages[] / システムプロンプト / ツール一覧**。最後のテキストしか親に返らない） | `.claude/agents/` 20 種 |
| 7 | Skills（起動時はカタログのみ、本文は `load_skill` で `tool_result` として渡す **2 段階ロード**） | `.claude/skills/` 17 種 |
| 8 | Context Compact（**安い順に 4 段階**。budget / micro は取り戻せるが snip は取り戻せない） | — |
| 9 | Memory（`MEMORY.md` を目次にし、**目次だけシステムプロンプトに置くことでキャッシュを壊さない**） | `~/.claude/.../memory/MEMORY.md` |
| 12 | Task System（`.tasks/{id}.json` に `blockedBy` / `owner`。`claim_task` は依存が完了していないと断る） | `.task-log/` |
| 15 | Agent Teams（**実体はファイル追記のメールボックス**。`{agent}.jsonl` へ 1 行 JSON を追記し、読んだら消す） | `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` |
| 18 | Worktree Isolation | 本組織は未使用（§5 参照） |

第 5 章の TodoWrite の説明が特に示唆的で、ツールの中身は「リストを受け取って表示するだけ」でファイルもコマンドも触らない。それでも効くのは、**モデルが `todo_write` を呼んだ瞬間に引数が `tool_use` ブロックとして `messages[]` に残る**から。状態はモデルが書いた側にあるので、戻り値は `"Updated 3 tasks"` で足りる。

### 4.2 エージェント向けツールは MCP より CLI（設計 3 原則）

**出典**: [AI エージェント向けのツールを、MCP ではなく CLI として作った — 設計の 3 原則](https://zenn.dev/codatum/articles/80b99faba75704)（g0e / Codatum、2026-08-04、curl 13,163 字）

MCP を試していた過程で CLI に行き着いた経緯と、その設計原則。**MCP を否定する記事ではない**と著者自身が断っている。

転換点は、MCP 経由でファイル検証を試したときの違和感。

> エージェントは、ローカルのファイルを読み込み、その内容を MCP ツールに渡すためのリクエストボディを組み立て、ツールを呼ぶ、という一連の処理を **すべて自分の「頭の中」＝コンテキスト上でこなそうとします**。

MCP のツールはファイルを**値渡し**でしか受け取れないため、1MB のノートブックを検証させたいだけでも 1MB がエージェントのコンテキストを通過する。CLI ならファイルそのものを引数にできる。

さらに、CLI は**人間・CI・エージェントが同じインターフェースに乗れる**。`cdm notebook validate` を CI で回すために MCP サーバーを立てて……とはならない。

**3 原則**:

| 原則 | 中身 |
|---|---|
| **1. コンテキストを浪費させない** | ファイル形式は**モデルの事前学習を活かせるもの**を選ぶ（JSON/YAML は括弧・インデント・キー反復にトークンを食う → Markdown 拡張を採用）。コマンド名も `validate` / `format` という既存概念に乗せる。独自構文を発明しない。ドキュメントは全部積まず `cdm doc index` / `doc show` で**取りに行かせる** |
| **2. 間違えさせない努力より、気づいて直せる設計に投資する** | プロンプトに禁止事項を積み上げるのをやめる。構造の誤りは `validate` で（**行番号 + 該当行の抜粋 + 具体的な指摘 + 直し方が載った URL** の 3 点セット）、見た目の誤りは PNG 出力させて**エージェント自身に目視確認**させる。機械的に直せるものはツールが直す（**LLM はランダム文字列の生成が苦手**なので ID は `format` が自動付与する） |
| **3. ワークフロー全体をシンプルな一本道に設計する** | 多段手順は 1 コマンドに畳み込む（ポーリングのループをエージェントの判断に委ねない）。検証は対象ごとに分けず `validate` 一発（**どれを叩くべきかという判断＝分岐を持ち込まない**）。流れは人間と共用のドキュメントで伝える |

原則 2 の前提として著者が強調しているのが、ドキュメントの位置づけ。

> エージェントに使わせるツールでは、ドキュメントは単なる付属物ではなく、**コマンドと並ぶインターフェースの一部**です。

原則 3 の「分岐を持ち込まない」は、cc-sier の Skill 設計に直接効く（§6 候補 C）。

### 4.3 「純粋リファクタリング」を証明させて自動マージする

**出典**: [「純粋リファクタリング」を AI に証明させて自動マージする](https://engineering.nature.global/entry/2026/07/prove-pure-refactoring-with-ai-and-auto-merge)（大塚 / Nature CTO、2026-07-31、curl 5,756 字）

3 層レビュー + auto-merge を持つ cc-sier にとって、**最も参考になる 1 本**。

出発点は、レビュー対象を**目的で分ける**という発想。リファクタリング PR で見るのは 2 軸しかない。

1. 設計が良くなっているか（命名・責任分割・依存の方向・結合度）→ **人間の判断が要る**
2. 挙動の等価性 → **機械的**

そこで前者は事前に GitHub issue や夕会で合意しておき、**後者だけを AI に判断させる**。AI に任せる範囲を限定することで信頼性を担保している。

実装は `review-refactoring` Skill + GitHub Actions。

1. PR に `/review_refactoring` とコメント
2. Actions 内で Skill 本体をそのままプロンプトとして Claude に渡し、ベースブランチとの差分をレビューさせる
3. **出力を JSON スキーマ（`pass` と `report_file`）で縛る**
4. `pass: true` → approve + squash auto-merge + `refactoring-only` ラベル
5. `pass: false` → auto-merge せず、人間レビューが必要とコメント

Skill の中身で挙げられているポイント 3 つ。

- **マインドセット**: 「あなたは懐疑的な第三者レビュアーである」「『たぶん大丈夫』ではなく『証明できる』レベルで検証せよ」——AI が人間に同意しがちなことへの対策
- **実行手順**: 動作変更が紛れ込みやすいパターンを列挙。**テストセットを少しずつ足して育てた**
- **原則**: テストコードの変化は挙動変化の兆候として扱う。**「他多数」「以下同様」といった省略を禁じ**、変更ファイル・関数・参照箇所をすべて列挙して 1 つずつ等価性をチェックさせる

さらにプロンプト自体も mizchi 氏の `empirical-prompt-tuning`（対象プロンプトを毎回まっさらな別エージェントに実行させ、どこが曖昧だったか・何手かかったかで評価して改善する Skill）で複数回チューニングしている。**CI で回すには flaky を避けるため曖昧さの除去が重要だった**という。

**実績（3 ヶ月半）**:

| 指標 | 値 |
|---|---|
| 自動マージしたリファクタリング PR | 247 件 |
| チーム全 PR に占める割合 | 約 3 割 |
| 2026 年 6 月の API 利用料 | $74.48 |
| 6 月の自動マージ件数 | 55 件 |
| **1 件あたり** | **約 $1.35** |

著者は前提条件も明記している。「積み上げてきた E2E テストの資産があるからこそ、安心して自動マージできる」。

### 4.4 コーディングエージェント安全運用ガイド v0.2

**出典**: [コーディングエージェントを安全に使うための実務ガイド v0.2](https://zenn.dev/kanaria007/articles/ee3dfd438af4df)（かなりあ、2026-08-07、curl 9,713 字）

15 節構成の実務ガイド。中心となる一文が置かれている。

> AI に証拠を構造化させ、機械で検出できるものは自動化し、人間は判断に集中する。

重要なのは**変更を「複雑さ」と「リスク」の別軸で扱う**設計。「単純でも高リスクな変更はあり、複雑でも影響を限定しやすい変更はある」。

本組織にとって効く節を 4 つ抜き出す。

**§5「PR を小さく分ければ安全になるわけではない」**

> 個別 PR がすべて正しいことと、統合された機能全体が正しいことは同じではない。
> merge conflict が発生しなかったことは、**意味上の競合が存在しないことを保証しない**。

**§9「プロンプトだけで安全を守ろうとしない」**

CLAUDE.md / rules / skills に「この操作は禁止してください」と書くのは有効だが、それは安全装置の最終層ではない、という指摘。

| 層 | 例 |
|---|---|
| リポジトリ | CODEOWNERS、branch protection、required checks |
| 実行環境 | sandbox、権限制限、network 制限 |
| CI | lint、typecheck、test、SAST、dependency scan、secret scan |
| レビュー | human review、security review、domain owner review |
| リリース | feature flag、canary、rollback 手順 |

> プロンプト → 望ましい振る舞いを伝える
> CI / 権限 / repository policy → 望ましくない変更を通さない

**§10「『問題ありませんか』より『証拠を出してください』」**

> `このPRはマージして大丈夫ですか？` という問いだけを投げると、最終判定まで AI へ委譲しやすい。

代わりに、各指摘へ「根拠となるファイルや処理 / 確認できた事実 / **未確認事項**」を添えさせ、AI の役割を**判定者ではなく証拠を収集・構造化する補助者**に寄せる。

**§12「統合後レビュー」** では、確認状態を 4 値で出させる形式が提示されている。

| 状態 | 意味 |
|---|---|
| CONFIRMED | 根拠と対応する証拠を確認できた |
| PARTIAL | 一部だけ確認できた |
| UNCONFIRMED | 十分な証拠が見つからない |
| CONTRADICTED | 計画や不変条件と矛盾している |

> 根拠のない推測で CONFIRMED にしないでください。

そして §13 のポイント。**AI に「人間が確認した」と書かせない。AI が作った証拠と、人間が行った判断を分ける。**

### 4.5 承認プロンプトの見逃し率は、脅威の種類で 3 倍違う

**出典**: [40 万件の AI 承認を分析したら、見逃し率が 3 倍違った — 危ないものほど、ちゃんと止められている](https://qiita.com/jqit_suwa/items/ac7d1201bd14e9a4e1ac)（jqit_suwa / 株式会社JQIT、2026-08-06、curl 4,422 字）

公開された 409,000 件の承認/拒否判断の集計。平均正答率 66.3%。

| 脅威の種類 | 見逃し率 |
|---|---|
| 明白な破壊的操作 | 11.7% |
| 永続的な変更 | 23.8% |
| 持ち出し・コード実行 | 33.4% |
| **スコープ違反** | **35.0%** |

**いかにも危なそうなコマンドは止められている**。止められていないのは、コマンド自体は正常なのに触ってはいけない範囲に手を出しているもの。

> 承認プロンプトに出てくるのは **コマンドそのもの** です。でも危険かどうかを決めるのは、**そのコマンドが何に対して実行されるか** のほう。
> `rm -rf ./build` → 危なそうに見える が、ビルド成果物なら問題ない
> `cp config.json /tmp/` → 普通に見える が、中身が API キーなら持ち出しになる

Anthropic のテレメトリでは**ユーザーは約 93% の許可プロンプトを承認**しており、「承認を目にする回数が増えるほど、ひとつあたりに払う注意は減っていく」という指摘も引かれている。

結論は「気をつけて読む」では解決しない、というもの。

> **スコープ違反は、人間に見せる前に潰す** しかありません。
> - 触ってよいディレクトリを設定で制限する
> - 本番環境の認証情報を、そもそも実行環境に置かない
> - 到達できないホストは、ネットワークレベルで遮断する

**著者はデータの限界を自分で 3 点挙げている**（査読された研究ではない / ゲームなので脅威の割合が現実より高い（約 34%）/ 被験者が自己選択）。それでも「種類によって 3 倍違う」傾向と Anthropic の実データが同じ方向を向く点は無視しにくい、と結んでいる。

---

## 5. 自組織での実測

記事の主張を、cc-sier リポジトリと手元環境の実測値と突き合わせた（2026-08-16 時点）。

### 5.1 実測結果一覧

| # | 記事の論点 | 本組織の実測値 | 判定 |
|---|---|---|---|
| 1 | `isolation: worktree` の不安定さ（§2.2） | `.claude/agents/` `.claude/skills/` `plugins/` のいずれにも `isolation` 記述**なし** | **該当なし**。ただし Workflow ツールに `isolation: "worktree"` オプションが存在するため、今後使う場合は §2.2 が予防知識になる |
| 2 | 並列サブエージェントの方が安い（§2.1） | `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 有効、Subagent 20 種。ただし**「独立タスクをまとめて 1 メッセージで投げる」ことは明文化されていない** | 🔶 部分該当 |
| 3 | Claude Code と Codex の設定が分岐する（§3.1） | **`AGENTS.md` は 2026-06-13 の 1 コミットのみ。同期間に `CLAUDE.md` は 19 コミット（最新 2026-08-08）** | ❗**分岐が発生している** |
| 4 | 同上（内容差分） | CLAUDE.md の `⚠️`/`❌` 注意事項 **28 件** に対し AGENTS.md は **0 件**。`curl` 原文取得ルール・`CLAUDE_CODE_DISABLE_BACKGROUND_TASKS`・`EXCESSIVE_PAGINATION`・「不確実さを分量で埋めない」等が AGENTS.md に**未反映** | ❗ |
| 5 | 同上（Skills 共有） | `~/.agents/` **存在しない**。`.claude/skills/` は Codex と未共有 | ❗ |
| 6 | Codex を実際に使っているか | `codex-cli 0.144.5` インストール済み、`~/.codex/config.toml` + `auth.json` あり、`goals_1.sqlite` が **2026-08-16 07:54 更新** | **現用中**。#3〜#5 は実害のある問題 |
| 7 | スコープ違反は承認画面前に潰す（§4.5） | `.claude/settings.local.json` は **`allow` のみで `deny` ブロックなし**。ディレクトリ制限の設定もなし | ⬜ 未対応 |
| 8 | 設定が静かに積もる（§2.4） | Skill **17 種**（SKILL.md 合計 183,775 bytes）、Subagent **20 種**（57,505 bytes）、rules **9 本**（44,203 bytes）、CLAUDE.md 14,964 bytes | 棚卸し未実施 |
| 9 | 同上（ドリフトの実例） | CLAUDE.md の「開発コマンド」表は **12 種**と記載するが実体は **17 種**。`company-cycle` / `company-diagram-v2` / `company-digest-insights` / `company-today` の **4 種が表に無い** | ❗ |
| 10 | 同上（同期ルール違反） | `plugins/cc-sier/skills/` に **`company-evolve` / `company-quality-setup` / `company-report` が存在しない**（`.claude/skills/` 側のみ。3 種とも git 管理下ではある） | ❗ **VCS 真ソース原則に反する** |
| 11 | cclens で計測（§2.4） | **未インストール**（`command -v cclens` → なし） | ⬜ |
| 12 | 純粋リファクタリングの自動マージ（§4.3） | L0/L1/L2 + auto-merge は稼働中。ただし**「リファクタリングのみ」という PR 分類は存在せず**、等価性判定の専用ゲートもない | ⬜ |

### 5.2 特に重い 3 件

**(a) AGENTS.md の 2 ヶ月放置（#3〜#6）**

Codex を現用しながら、Codex 側が読む `AGENTS.md` は 6 月 13 日から一度も更新されていない。この間に CLAUDE.md へ入った注意事項——`curl` での原文取得ルール、`claude-code-action` の env 配置、Projects v2 の pagination 制限、「不確実さを分量で埋めない」——は**すべて Codex には届いていない**。§3.1 の記事が「独自に分岐していく未来が容易に想像できる」と書いた状態が、すでに 2 ヶ月分進行している。

なお `AGENTS.md` は CLAUDE.md から `@AGENTS.md` 参照もされていないため、Claude Code 側からも読まれていない。**現状は誰も読んでいないファイルとして 6,366 bytes 存在している。**

**(b) Skill 台帳のドリフト（#9, #10）**

CLAUDE.md の記述（12 種）と実体（17 種）が乖離し、うち 3 種は VCS 真ソースである `plugins/` 側に存在しない。`@.claude/rules/skill-development.md` は「`plugins/cc-sier/skills/` が VCS 真ソース。`.claude/skills/` 側に独自編集を入れない」と定めているが、**3 Skill がそのルールの外にある**。

cclens の言う「気付いたら使われない設定だけが静かに積もっていく」の実例だが、本件は計測ツールがなくても `ls` と `grep` で検出できた。**まず必要なのはツールではなく棚卸しの習慣**である。

**(c) permission に deny が無い（#7）**

`settings.local.json` は allow リストのみで、deny もディレクトリ制限もない。§4.5 のデータに照らすと、見逃し率が最も高いのは「コマンド単体では正常に見えるスコープ違反」であり、これは承認プロンプトで人間が止められるものではない。ただし本組織は**単一リポジトリ・単一開発者・本番認証情報を実行環境に置いていない**ため、記事が想定する被害シナリオの一部は成立しない。優先度は §6 で中とした。

### 5.3 付随発見（本解析の対象外）

`.mcp.json` の `awslabs.aws-iac-mcp-server` が `@latest` 指定で、CLAUDE.md 自身の「uvx/npx 系 MCP サーバーは `==バージョン` でピン留め」（#569/#570 の教訓）に反している。本解析の範囲外のため**指摘のみで対処していない**。

---

## 6. 採用候補

優先度は「本組織で実害が出ているか」と「対処コスト」で決めた。

### 候補 A: AGENTS.md を CLAUDE.md へ畳む — 優先度 **高**

**根拠**: §5.2 (a)。Codex 現用中に 2 ヶ月分の分岐が発生している。

**やること**（§3.1 の手法をそのまま適用）:

1. CLAUDE.md に `@AGENTS.md` 記載を入れるか、逆に **AGENTS.md を削除して CLAUDE.md に一本化**する
2. Skills を Codex へ渡すなら `.claude/skills/` → `.agents/skills/` の symlink（git は symlink をパス文字列として記録するため版管理できる）
3. **ただし記事の警告に従い、一律同期はしない**。Anthropic と OpenAI で推奨される指示の書き方が異なるため、rules については「ファイル一覧と読むべきタイミングだけを AGENTS.md に書き出す」方式を採る

**コスト**: 小（1 セッション）。**判断が要る点**: 一本化するか symlink 併存にするかは、笹尾さんが Codex をどの程度使い続けるかで変わる。

### 候補 B: Skill 台帳の棚卸し — 優先度 **高**

**根拠**: §5.2 (b)。CLAUDE.md の記述（12 種）と実体（17 種）の乖離、`plugins/` 未同期 3 種。

**やること**:

1. `company-evolve` / `company-quality-setup` / `company-report` を `plugins/cc-sier/skills/` へ移送（VCS 真ソース原則の回復）
2. CLAUDE.md の開発コマンド表に 4 種を追加し、「12 種類」の記述を修正
3. `diff -rq plugins/cc-sier/skills/ .claude/skills/` を定期実行できる形にする

**コスト**: 小。**これは記事由来ではなく、記事を読んだことで探しに行って見つかった問題**である。

### 候補 C: `/company-*` Skill に「分岐を持ち込まない」原則を適用 — 優先度 **中**

**根拠**: §4.2 原則 3。「どれを叩くべきかの判断こそが分岐で、抜けや取り違えの温床になる」。

cc-sier の Skill は 9 フェーズ構成で、L0 の検証手段が Skill ごとに異なる（`review-drawio.js` / `validate_drawio.py` / grep）。§4.2 の主張に照らすと、**検証コマンドを 1 本に寄せ、何を編集したかにかかわらず同じものを叩く**形が望ましい。

**未確認**: 現行 9 フェーズで実際に「どの検証を叩くべきか」の取り違えが起きているかは**測定していない**。まず task-log の `l0_retries` を集計して発生実績を確認すべきで、実績がなければ着手しない。

### 候補 D: 独立タスクは 1 メッセージで並列起動する運用の明文化 — 優先度 **中**

**根拠**: §2.1。直列サブエージェント経由（119,194 トークン）に対し並列（66,884）で 44% 減。

**やること**: `@.claude/rules/` または該当 Skill に「独立した委譲が複数あるときは 1 メッセージにまとめて起動する」を明記。

**留保**: 元データは 3 タスク・1 セットの測定で、査読も反復もない。本組織の Agent Teams は日次ダイジェストのように**重く独立性の高い**タスクで使っており、記事の測定条件（`tool_uses: 0` の一発回答）とは異なる。**同じ比率で効くとは限らない。**

### 候補 E: `deny` とディレクトリ制限の追加 — 優先度 **中**

**根拠**: §4.5。スコープ違反の見逃し率 35.0%、承認率 93%。

**やること**: `settings.local.json` に `deny` を追加し、`.companies/{他組織}/` への書き込みや `~/.ssh` 等の読み取りを構造的に止める。

**留保**: 単一開発者・本番認証情報なしの環境では、記事が想定する被害の一部は成立しない。ただし**組織横断の誤書き込み**（`@.claude/rules/multi-org.md` の「組織 A の作業で組織 B に書き込まない」）は現状プロンプトでしか守られておらず、これは §4.4 §9 の「プロンプトだけで安全を守ろうとしない」に該当する。

### 候補 F: cclens の導入 — 優先度 **中**

**根拠**: §2.4。Skill 17 種 / rules 9 本 / Subagent 20 種の効果検証が未実施。

**やること**: `brew install lambdalisue/cclens/cclens` → `cclens doctor` → `cclens overhead` で常時オンのコンテキスト量を、`cclens usage` で Skill 別使用回数を確認。

**留保**: **Windows 非対応**と記事に明記されている。本環境は WSL2（Linux）なので動く見込みだが、**未検証**。また候補 B の問題は cclens なしで見つかったので、**ツール導入を棚卸しの前提条件にしない**。

### 候補 G: 純粋リファクタリング判定ゲート — 優先度 **低**

**根拠**: §4.3。3 ヶ月半で 247 件自動マージ、1 件あたり約 $1.35。

**不採用寄りの理由**: Nature の前提は「積み上げてきた E2E テスト資産」であり、cc-sier は**主にドキュメント・図・HTML を生成するリポジトリでコードの自動テストがほとんどない**。挙動の等価性という概念自体が成立しにくい。

**ただし転用できる部分がある**:

- 出力を **JSON スキーマで縛る**（cc-sier の L2 はすでに JSON 出力だが、スキーマによる強制はしていない）
- **「他多数」「以下同様」の省略を禁じ、全件列挙させる**（L2 review-prompt に追加可能）
- `empirical-prompt-tuning` で review-prompt 自体を育てる

### 候補 H: L2 レビューを「判定」から「証拠の構造化」へ寄せる — 優先度 **中**

**根拠**: §4.4 §10 §12 §13。

現行の L2 は `verdict: pass|fail` と `composite` を返す**判定者**である。§4.4 の主張に従えば、AI には CONFIRMED / PARTIAL / UNCONFIRMED / CONTRADICTED の 4 値で確認状態を出させ、最終判定は人間と機械（L0）に残すのが筋。

**ただしこれは cc-sier の auto-merge 設計の根幹に触れる**。無人で回す前提を崩すことになるため、**変更するかは方針判断であって、記事が正しいから直す類の話ではない**。まず「L2 の findings に未確認事項の欄を追加する」という部分適用から試すのが現実的。

### 候補 I: plan モードをやめて「案をください」 — 優先度 **低**

**根拠**: §2.5。

**留保が大きい**。記事の前提は「賢いモデルに任せる雑な命令」だが、cc-sier は Skill が固定フェーズと必須セクションを持つ設計であり、方向性が逆。**個人の対話作業には効く可能性があるが、Skill 経由の作業には適用しない**のが妥当。

### 候補 J: AgentCore Gateway による Codex の Budget 制御 — 優先度 **低（時期尚早）**

**根拠**: §3.2。1 人運用ではユーザー単位のコスト帰属は過剰。要件定義書 §1.3 の Phase 2（チーム展開）に入る段階で再評価する。

---

## 7. 未検証事項

本レポートで**確認していないこと**を明示する。

| # | 未検証の内容 | 影響 |
|---|---|---|
| 1 | ~~08-10〜08-15 の 6 日分（196 件）を読んでいない~~ → **§8 で追補済み**（2026-08-16）。当初「存在しない」と書いたのは `git fetch` を怠った誤り（§0.1 参照） | 解消。ただし追補の原文精読率は 4.8%（§8.7 #1） |
| 2 | 母集団 181 件のうち、精読したのは 12 件（6.6%）。残り 169 件は**タイトルと 1 行要約しか読んでいない** | 見落としの可能性がある。特にクラスタ I（ナレッジ表現 11 件）は 3 レンズに乗らないと判断して切ったが、判断根拠はタイトルのみ |
| 3 | §2.1 の並列サブエージェント測定を**本組織で再現していない** | 44% 削減が cc-sier の Agent Teams でも成立するかは不明 |
| 4 | §2.3 の「要約が選択中のモデルで走る」「5 時間ウィンドウ側の節約」は**記事の著者自身が推論と明記**しており、本レポートでも検証していない | `/model` を落として `/compact` する手順の効果は未確認 |
| 5 | cclens を**インストールしていない**（WSL2 での動作も未確認） | §2.4 の各コマンドの出力は記事の記述のみに基づく |
| 6 | `plugins/` 未同期 3 Skill について、**`.claude/skills/` 側の内容が正か、移送時に何が起きるかを確認していない** | 候補 B の作業量は未見積もり |
| 7 | 候補 C の前提となる `l0_retries` の集計を**していない** | 分岐による取り違えが実際に起きているかは不明 |
| 8 | §4.1 の教材リポジトリ `shareAI-lab/learn-claude-code` の**原典は読んでいない**（解説記事のみ） | 各章のコードの正確性は解説記事に依存 |
| 9 | AGENTS.md と CLAUDE.md の**全文差分を取っていない**（キーワード 9 個の有無と注意事項件数で判定） | 分岐の全容は未把握 |
| 10 | 候補 A〜J は**いずれも未実施**。本レポートは解析のみで、リポジトリへの変更は行っていない | — |

---

## 8. 追補 — 2026-08-10（月）〜 08-15（土）

### 8.1 追補の経緯と方法

§0.1 の訂正のとおり、当初「未生成」と誤認した 6 日分が実在した。オーナー判断により同じ 3 レンズで追補する。

| 項目 | 値 |
|---|---|
| 対象 | 2026-08-10 〜 08-15 の A1 / A2 章 |
| 母数 | 記事 196 件 / ユニークタイトル 168 件 |
| 精読 | **8 件**（4.8%。すべて `curl` で原文取得） |
| 起動 | **`/company-insights-cycle`（本 Skill の初回通し実行）** |

Phase 0 で **`git fetch --prune` を実行した**（本編で怠って取りこぼしを生んだ手順）。

精読 8 件は「本編の記述を延長または反証するもの」を基準に選んだ。選定理由を各節に書く。**残り 160 件はタイトルと 1 行要約しか読んでいない。**

---

### 8.2 レンズ① Claude Code 実運用

#### 8.2.1 「CLAUDE.md は短い方が良い」を 1,650 回試したら、長さは効かなかった

**出典**: [「CLAUDE.md は短い方が良い」を 1,650 回試して確かめた](https://zenn.dev/odacchi/articles/claude-md-length-experiment)（odacchi、2026-08-12、curl 8,619 字）

**選定理由**: 本編 §2.4 と既存カタログの**候補 Q（CLAUDE.md / rules の削減）に直接決着をつけられる**唯一の実測記事。8/1〜8/8 の解析では「公式が短く保てと言っている」という規範の話しかなかった。

著者は 228 行と 1,040 行の CLAUDE.md で、同じルールを思い出せる度合いを比較した（合計 1,650 回）。

| 環境 | 指数 | 95% 信頼区間 |
|---|---|---|
| CLAUDE.md 228 行（基準） | 100.0 | 94.6 – 105.7 |
| 1,040 行 — ルールを**中ほど**に埋める | 102.7 | 97.3 – 108.3 |
| 1,040 行 — ルールを**先頭**に置く | 104.7 | 99.8 – 110.1 |

**落ちない。** 並べ替え検定で p = 0.219、3 条件間に差があるとは言えない。

指標の感度は対照で担保されている。**該当ルールの本文だけを抜いた環境では指数 4.5（1/22）まで落ちる**ため、「差を検出できない鈍い指標」ではない。

さらに 3 点が測られている。

| 検証 | 結果 |
|---|---|
| CLAUDE.md 一枚岩 vs `.claude/rules/` 分割 | **差なし**。`paths` を持たないルールファイルは CLAUDE.md と同じ書式でシステムプロンプトに展開されるため（実測でプロンプト文字列の完全一致を確認） |
| コンテキスト 28k → 198k トークン | **落ちない**（分割配置の信頼区間 ±0.03） |
| 分割のトークンコスト | 27 ファイルで **+1,314 トークン（3.1%）**、全量がファイルパスのヘッダ。1 ファイル約 50 トークン |

著者の結論は、効かないものばかり見つかったので **「効くのは量ではなく整合性ではないか」** という仮説である。

> 同じことを別の場所で違う言い方で書いている / 上位スコープと下位スコープで指示が食い違っている / 古い手順が消されずに残り、新しい手順と併存している
> こうなると、モデルはどちらに従うか決められません。**量を減らしても矛盾は減りませんし、逆に量が多くても一貫していれば問題ない**

**著者が自分で挙げた限界**（そのまま重い）:

- 測ったのは **想起**（聞かれたら答えられるか）であって **遵守**（実作業中に実際そう動くか）ではない
- 1,040 行まで。1 万行規模は不明。コンテキストは約 20 万トークンまで
- **水増しに使ったのは対象ルールと矛盾しないルール**。つまり「矛盾した文章が混ざることの害」は測っていない（＝上記の仮説自体は未検証）
- n=138〜210 で検出できる差は 5〜8 ポイント程度。「差が無い」ではなく「この規模で検出できる差は無い」

分割する理由として残るのは 3 つだけ、とされる。`paths` による条件ロード / diff を閉じる / 人間が読みやすい。

#### 8.2.2 サブエージェントの fork が既定で有効になった（v2.1.232）

**出典**: [Claude Code v2.1.230〜v2.1.232 の主要アップデート](https://dev.classmethod.jp/articles/20260814-cc-updates-v2-1-232/)（石川覚 / クラスメソッド、2026-08-14、curl 13,882 字）

**選定理由**: 本編 §2.1（並列サブエージェントはキャッシュ共有で 44% 安い）の**前提そのものが変わった**ため。

> `subagent_type: "fork"` を指定したサブエージェントは**会話全体とプロンプトキャッシュを引き継ぎます**。あわせて、対話セッションでの teammate 以外のエージェント起動が既定でバックグラウンド実行になりました。

従来のサブエージェントは "starts a fresh agent with no memory of prior runs" で、①前提の再入力 ②**プロンプトキャッシュのミス**（別プレフィックスになる）③文脈の取りこぼし、の 3 課題があった。fork はこれを直接解く。

著者はモデルに渡る Agent ツールの説明文が差し替わることを実測で確認し、さらに親セッションしか知らない合言葉（「ひまわり0814」）を fork サブエージェントが答えられることを確かめている。

従来動作に戻すには `export CLAUDE_CODE_FORK_SUBAGENT=0`。

なお同回は 50 件と規模が大きく、**権限バイパス 2 件（PowerShell / Git Bash のシンボリックリンク）とネストした git リポジトリの信頼継承**が修正されている。破壊的変更として `sandbox.ripgrep` がプロジェクト設定から指定できなくなった。

#### 8.2.3 承認の「auto mode」が既定になった — 本編 §4.5 への Anthropic の回答

**出典**: [Claude Code、承認ボタンを何度も押さなくていい「自動モード」がデフォルトに](https://pc.watch.impress.co.jp/docs/news/2131930.html)（宇都宮充 / PC Watch、2026-08-10、curl 9,798 字）

**選定理由**: 本編 §4.5（承認プロンプトの見逃し率はスコープ違反で 35.0%）と**同じ問題に対する提供側の回答**であり、しかも既定値の変更なので放っておいても適用される。

2026-08-14 から、Pro / Max / Team の新規セッションの権限モード既定が **auto mode** に変更された。すべてのツール呼び出しを分類器がチェックし、危険な操作を検知・ブロックする。**分類器で消費されるトークンは無料**。

Anthropic が示した数値（1,053 人のテスター）:

| | 危険なコマンドの検知率 |
|---|---|
| **人間のテスター** | **13.6%** |
| **auto mode** | **89%** |

さらにセッション長の影響が明示されている。

> テスターの場合、危険なコマンドの検知率はセッション初期で約 17% だったが、承認プロンプトを 50 回以上処理したあとでは**約 5% にまで低下**した。これに対し、**自動モードはセッションの長さに関わらず、一定の検知率を維持**した。

第三者機関のプロンプトインジェクションテストでは、auto mode の Fable 5 / Opus 5 / Sonnet 5 が計 720 件の攻撃を**すべてブロック**。OpenAI Codex（GPT-5.6 Sol）は自動レビューモードでも攻撃成功率 5.83% だったとされる。

**注意**: 本記事はニュース記事であり、**数値はすべて Anthropic 自身の発表に基づく**。第三者による再現は確認していない。本編 §4.5 の 409,000 件データ（査読なし・ゲーム由来）とは出所が異なるが、**「回数を重ねるほど注意が落ちる」という傾向は両者で一致**している。

#### 8.2.4 worktree フックで使い捨て隔離環境を作る — §2.2 の対抗案

**出典**: [Claude Code の worktree フックで作る使い捨て隔離開発環境](https://zenn.dev/cilly/articles/claude-code-worktree-isolated-env)（cilly、2026-08-10、curl 9,857 字）

**選定理由**: 本編 §2.2 は「`isolation: worktree` をやめてシェルスクリプトにした」話だった。本記事は**やめずに、フックで組む**逆の結論に至っており、性質の異なる 3 プロジェクトで運用した記録がある。

結論は §2.2 と同じ方向を向いている。

> **実処理は決定論 CLI に集約し、スキルは「翻訳係」に徹する。** 環境の作成・採番・起動・破棄というオーケストレーションを、LLM に手順として実行させない。

スキル定義の中身は「ユーザーの言い方 → コマンド」の対応表だけ。§2.2 の「単体で叩いて確かめられるか」と同じ物差しである。違うのは、`git worktree` の**呼び出し口**を Claude Code のフックに寄せた点。

| フック | 役割 |
|---|---|
| `WorktreeCreate` | worktree 作成そのものを乗っ取る。stdout にパスだけを出す契約 |
| `SessionStart` | 停止中の環境を自動 start。**stdout がコンテキストに注入される**ので接続情報（URL / DB）を流し込める |
| `SessionEnd` | 自動 stop。**`/clear` でも発火する**ため `reason` で分岐しないと作業中に環境が落ちる |

「衝突しない」は固定値を捨てて手に入れる、という 3 世代の進化も具体的である。共有レジストリの連番採番は**ロックがなく同時作成で全衝突**して破綻し、最終的に**内容由来 ID（タスクキー + パスの短縮ハッシュ）+ 動的ポート採番**に落ち着いた。動的に決まった値は `runtime.json` に記録し、破棄はその記録を基に行う。

不変条件として「メイン環境に一切書き込まない」「破棄で全部消えるがブランチは残す」「平文シークレットを worktree に複製しない」を**スキル定義に明記して、エージェントによる改変から守っている**。

---

### 8.3 レンズ② Codex 併用

#### 8.3.1 claudex — Claude Code のハーネスで Codex を動かす

**出典**: [Claude Code経由でCodexを使う「claudex」の導入・検証レポート](https://zenn.dev/yam2357/articles/claudex-proxy-codex)（山田、2026-08-10、curl 13,640 字）

**選定理由**: 本編 §3.1 の記事が「`claudex` などで調べてください」と一行だけ触れていた構成の、実測を伴う検証。

Claude Code の CLI・ツール実行・エージェント機構をそのまま使い、モデル呼び出しだけをローカルプロキシ（CLIProxyAPI）で Anthropic Messages API 形式 → Codex 形式に変換して `gpt-5.6-sol` に中継する**非公式**構成。

**リスクが冒頭に明記されている。** OpenAI の Tibo Sottiaux 氏が公開した手順をほぼそのまま試した利用者が、直後に Anthropic によるアカウント停止を報告した。Claude Code 責任者の Boris Cherny 氏が「他モデルとハーネスを併用したことを理由に禁止することはなく、別のアカウント分類器がトリガーとなった可能性が高い」と応答し、約半日で解除された。著者の評価は「意図的な BAN ポリシーではなく個別に是正された誤検知」だが、**「リスクがゼロになったわけではない」**と結んでいる。

検証結果で重要なのは、**失敗がモデル品質の問題ではなかった**こと。

| 構成 | 有効回答率 |
|---|---|
| 標準テキスト出力 | **5/12（42%）**。読み取りツールを使う 2 タスクは **0/6（0%）** |
| 構造化ストリーム出力 | **11/12（92%）**。同じ 2 タスクは **6/6（100%）** |

原因はヘッドレスモードの標準 text 出力の不安定性で、セッショントランスクリプトには正しい最終テキストが保存されていた。`result` イベントの `subtype: success` を抽出する方式で回避できる。

**本組織にとって効くのは、むしろ付随して公開されている「委譲マトリクス v3」**である。原則は次の一文。

> 同じ水準で実行できるなら、**Claude プランを消費しない Codex へ寄せる**。cc を標準にするのは、**親の対話文脈と密結合した作業、または外部に出せない機密データを扱う作業だけ**とする。

判断は 2 問。(1) 親セッションの対話文脈と強く結び付くか (2) 外部に出せない機密データか。どちらかが Yes なら Claude Code、両方 No なら Codex。ただし**高リスクレビュー（認証・権限・課金・データ移行・並行処理・公開 API・不可逆インフラ）は両方を独立並列で走らせ、互いの結果を見せない**。

**著者自身が限界を明記している**: 実験2 はセッション記録 1 件、実験3 は 4 パターン×各 1 回で「統計的有意性を主張する規模ではない」、判定者にも執筆者と同系列という評価者バイアスがある。

---

### 8.4 レンズ③ AI エージェント設計

#### 8.4.1 「違反できないコード」を書かせる — ガイドとセンサーの設計

**出典**: [Claude Codeに「違反できないコード」を書かせる：ガイドとセンサーの設計](https://qiita.com/shun123/items/7aa06b6982dfd1730af0)（shun noel / JISOU、2026-08-11、curl 34,401 字）

**選定理由**: 本編 §4.4 §9「プロンプトだけで安全を守ろうとしない」を、**設定ファイル単位まで具体化した唯一の記事**。本編は層の一覧までで、実装が無かった。

問題設定が cc-sier に刺さる。

> CLAUDE.md は読まれますが、従われるとは限りません。（中略）**「守ってほしい」と書くことと「守られている」ことは別物**という構造の問題です。
> 口頭で直すたびに、あなたが規約の一部を肩代わりしていることになります。

ハーネスを 2 つに分ける。

| 分類 | 実行するもの | 性質 |
|---|---|---|
| **ガイド**（フィードフォワード） | 設定・文書 | 書ける範囲そのものを狭める |
| **計算的センサー** | CPU 上のプログラム | 速い。同じ入力なら必ず同じ結果 |
| **推論的センサー** | LLM | 遅い。結果がぶれる。**意味の判断ができる** |

**cc-sier の L0（機械）/ L2（LLM）とほぼ同じ二分**だが、本記事は「先に計算的センサーを作り、その限界を自分の目で確かめてから推論的センサーを作る」順序を強く推している。

実装で持ち帰れるものが 3 つある。

**(a) `deny` の具体設計** — `defaultMode: "dontAsk"` + allow 列挙 + deny。deny の対象が示唆的である。

> `node -e` や `sh -c` は引数の文字列を実行するため、`rm` を禁止しても**迂回経路**になる
> `git -c キー=値` はそのコマンド 1 回だけ設定を渡す書き方で、設定ファイルは変更しないが同じ効果を持つ。**片方だけ禁止しても意味がない**

また `Write(path)` を書いたルールは**受理されるが参照されない**（ファイル権限の照合は `Edit(path)` と `Read(path)` のみ）という実装上の注意も書かれている。

**(b) エラーメッセージは修正手順である**

> 人間向けの lint 設定なら「禁止です」で足ります。AI に自己修正させるなら、「どこに何を定義して、どう注入するか」まで書きます。**メッセージの質が、そのまま自己修正の成功率になります。**

**(c) 「通った」は「検査された」を意味しない** — ファイルが 1 つも無い状態でも成功する検査が 5 つ中 3 つあった、という実測を示している。これは既存カタログの候補 S（緑 ≠ 実用可）と同じ主張の、より原始的な形である。

さらに手順 9 で「わざと違反コードを 3 種類書かせ、検出されるか試す」工程を置いている点が重要で、**センサー自体をテストしている**。

#### 8.4.2 同じ指摘を二度させない — count で強制力を段階的に上げる

**出典**: [Claude Code に「同じ指摘を二度させない」仕組みを hook で作った](https://zenn.dev/nozomi720/articles/claude_code_hooks_feedback)（nonchan7720、2026-08-11、curl 9,506 字）

**選定理由**: cc-sier の Case Bank / feedback memory と**同じ問題を、強制力の側から解いている**。

1 指摘 = 1 ファイル（`~/.claude/feedback/{topic}.md`）に永続化し、frontmatter の `count`（指摘された回数）から `severity` を自動決定する。

| count | pre_bash / pre_edit | stop_check |
|---|---|---|
| ≥ 5 | **deny**（ツール実行を禁止） | deny |
| 3〜4 | ask（ユーザーに確認） | block |
| 1〜2 | warn（stderr に出すだけ） | warn |

> 1〜2 回目は「記録するが縛らない」暫定ルール。3 回目で確定ルールになり、5 回目で問答無用の禁止になります。**指摘の重みは、指摘した回数という一番正直な指標で決まる**わけです。

設計上の判断が 3 つ効いている。

- **`count` は自動インクリメントしない。** hook が検知した違反は「Claude が破ろうとした」だけで「人間がもう一度指摘した」わけではないため。**ルールの重みを上げる権限は人間側に残す**
- **本文に「言い訳」を書かせる。** ルールを破りたくなる場面で先に立つ理屈を、あらかじめ潰した文章を同じファイルに置く
- **hook 自身は絶対に事故らせない。** 3 つの hook すべてで例外を握り潰して `exit 0`。「検知できないことより止まらないことを優先」

コンテキスト圧迫対策として、注入は 3,000 文字の予算を設け、**超えたら `count` の低いものから本文を落として description だけにする**（重いルールほど詳しく残る優先度付き切り捨て）。

**著者が挙げた限界**: 47 ルール中 `enforce` を書けたのは 14 個だけ。「実装を先に書いてから呼び出し元を書く」のような**順序のルールは、ファイル単体を見ても違反判定ができない**。

#### 8.4.3 Agent Plugins 1.0.0 — 何を標準化し、何を標準化しなかったのか

**出典**: [Agent Plugins 1.0.0 は何を標準化し、何を標準化しなかったのか](https://zenn.dev/inariku/articles/agent-plugins-1-0-deep-dive)（いなりく / AWS Japan、2026-08-10、curl 17,364 字）

**選定理由**: 本編のクラスタ F（エージェント向けツール設計 8 件）で精読を見送った領域。**cc-sier 自身がプラグイン形式である**ため、標準への準拠が論点になる。

まず前提の整理が有用である。**Agent Skills（Anthropic 発、中身の標準）と Agent Plugins（OpenAI 主導、箱の標準）はレイヤーが違う。**

v1 が定義するのは「`plugin.json` + `skills/` + `mcp.json` を入れた、検査可能なディレクトリ」だけ。**hooks・subagents・rules・スラッシュコマンド・LSP 設定はすべて意図的にスコープ外**である（Design Decisions いわく「クライアント固有性が強すぎて、安定したポータブル契約にはまだ早い」）。

設計として学べる点:

- **closed schema + `extensions` 名前空間** — トップレベルに書けるのは 10 フィールドだけ。ベンダー独自設定は逆ドメイン名前空間へ隔離する
- **階層化された非致死エラー** — サーバー 1 つの障害で「全部入りプラグイン」が全損しない。知らないフィールドは警告して無視（前方互換）、必須欠落は拒否
- **`PLUGIN_ROOT` / `PLUGIN_DATA` の分離** — 更新で差し替わるパッケージと、更新をまたいで保持される状態を最初から分ける
- **`command` はシェル文字列ではなく単一の実行可能トークン** — 引数は `args` 配列に分離。コマンドインジェクションの余地を構造的に減らす
- **「配布物の中に秘密を入れる標準的な方法は存在しない」と仕様レベルで断言**

そして本組織に直接効くのが次の事実である。

> **TSC に参加していない Claude Code は公式の対応クライアント一覧に載っていない**
> Claude Code のプラグインは独自形式のまま。マニフェストは `.claude-plugin/plugin.json`（標準はルート直下の `plugin.json`）

将来検討リスト（FUTURE_CONSIDERATIONS.md）を占めるのは、権限・署名・シークレット・組織統制・監査——つまり**信頼とガバナンス**であって、新コンポーネントの追加ではない。

著者の結論は「**いま投資すべきレイヤーは Agent Skills**。スキルとして書いた知見は、標準の勝敗がどう転んでもポータブル」。

---

### 8.5 追補の実測（2026-08-16 時点）

| # | 記事の論点 | 本組織の実測値 | 判定 |
|---|---|---|---|
| 1 | CLAUDE.md の長さは想起に効かない（§8.2.1） | **`CLAUDE.md` 208 行 / `.claude/rules/` 9 本 合計 1,010 行** | ❗ **記事の実験条件（228 行 vs 1,040 行）とほぼ一致**。記事が「差なし」と結論づけた範囲に本組織はぴたりと収まる |
| 2 | 効くのは量ではなく整合性（同じ内容の重複・スコープ間の食い違い） | 同一知見の記載箇所: `task-log` **8 ファイル** / `auto-merge` **5** / `plugins/cc-sier/skills` **5** / `main 直コミット` **4** | ❗ 重複が広範。ただし**矛盾しているかは未判定**（§8.7） |
| 3 | 同上（古い手順の併存） | **`AGENTS.md` が上記 7 キーワード中 5 つに登場**。しかし 2026-06-13 から未更新 | ❗ **記事が名指しする「古い手順が消されずに新しい手順と併存」そのもの** |
| 4 | 同上（上位スコープとの食い違い） | ユーザーレベル `~/.claude/CLAUDE.md` **なし**。auto-memory 7 ファイル | 低リスク |
| 5 | fork 既定化（§8.2.2） | **Claude Code 2.1.233**（fork 既定化は v2.1.232） | **既に適用済み**。`CLAUDE_CODE_FORK_SUBAGENT` は未設定 |
| 6 | auto mode 既定化（§8.2.3） | `settings.json` / `settings.local.json` に **`defaultMode` の指定なし** | **2026-08-14 以降 auto mode で動いている**（明示設定していないため） |
| 7 | `deny` の設計（§8.4.1） | `settings.local.json` は **allow のみ、`deny` ブロックなし** | ⬜ 本編 §5.1 #7 と同じ。記事が具体設計を提供 |
| 8 | count による段階的強制（§8.4.2） | `~/.claude/feedback/` **0 件**（未導入）。auto-memory に feedback 系 7 ファイル | 🔶 記録はあるが**強制力に接続されていない** |
| 9 | Agent Plugins 1.0.0 準拠（§8.4.3） | `.claude-plugin/plugin.json` **あり** / ルート直下 `plugin.json` **なし** | **非準拠**（Claude Code 独自形式。記事のとおり Claude Code 自体が非対応） |
| 10 | worktree フック（§8.2.4） | `WorktreeCreate` / `SessionStart` フック**未使用**（hooks は PostToolUse / Stop の 2 種） | 該当なし |

#### 8.5.1 最も重い発見 — 候補 Q の前提が崩れた

既存カタログの **候補 Q（CLAUDE.md / rules の削減）は「長さが精度を下げる」という前提**で優先度「中」に置かれていた。§8.2.1 の実測により、**その前提は本組織の規模（CLAUDE.md 208 行 + rules 1,010 行）では成り立たない**。

代わりに測るべきは**整合性**である。#2 と #3 の実測は、記事が挙げた 3 つの矛盾パターンのうち 2 つ（同じことを別の場所で書いている / 古い手順が新しい手順と併存）に本組織が該当していることを示す。

**ただし記事自身が「矛盾した文章が混ざることの害は測っていない」と明記している。** 整合性が効くという仮説は、記事でも本レポートでも**未検証**である。

---

### 8.6 追補の採用候補

| ID | 候補 | 優先度 | 根拠 |
|---|---|---|---|
| **AO** | **候補 Q を「長さの削減」から「整合性の是正」へ組み替える** | **高** | §8.2.1 の 1,650 回実測。本組織は 208 行 + 1,010 行で、記事が差なしと結論づけた範囲内。**削る作業をやめ、AGENTS.md の畳み込み（候補 AD）と重複の解消（候補 O）に振り替える** |
| **AP** | `deny` の具体設計を導入する | **高** | §8.4.1。候補 AH の実装指針が得られた。特に **`node -e` / `sh -c` / `git -c` の迂回経路**と、`Write(path)` ルールが参照されない仕様は自力では気づけない |
| **AQ** | fork を前提にサブエージェントの投げ方を組み直す | 中 | §8.2.2。**既に 2.1.233 で適用済み**なので、判断は「使うか」ではなく「前提を書き換えるか」。本編候補 AG（並列起動の明文化）は fork 前提で書き直す必要がある |
| **AR** | auto mode 既定化を前提に権限設計を見直す | 中 | §8.2.3。**すでに auto mode で動いている**（#6）。分類器 89% を信頼するのか、`deny` で構造的に潰すのか（候補 AP）の方針決めが要る |
| **AS** | 指摘の `count` を強制力に接続する | 中 | §8.4.2。Case Bank / feedback memory は記録止まりで、warn → ask → deny の段階的強制が無い。**`count` を人間だけが上げる**という設計は本組織の「AI が自分を縛るルールを一方向に増やす」問題（候補 AA）への対処にもなる |
| **AT** | Agent Plugins 1.0.0 準拠は**見送る** | ❌ | §8.4.3。**Claude Code 自体が標準に非対応**で、cc-sier は Claude Code 上でしか動かない。準拠しても得るものがない。ただし記事の結論「投資すべきレイヤーは Agent Skills」は本組織の方向と一致しており、**Skill の可搬性を保つ**方針は維持する |
| **AU** | claudex の導入は**見送る**が、委譲マトリクスの考え方は採る | 低 | §8.3.1。非公式構成でアカウント停止事例あり（誤検知として解除済みだが再発保証なし）。一方 **(1) 親文脈と密結合か (2) 機密データか の 2 問で振り分ける**判断基準は、Codex を現用する本組織にそのまま使える |

---

### 8.7 追補で確認していないこと

| # | 未検証の内容 | 影響 |
|---|---|---|
| 1 | **168 件中 8 件（4.8%）しか原文を読んでいない**。残り 160 件はタイトルと 1 行要約のみ | 本編（6.6%）よりさらに低い。見落としの可能性は本編より大きい |
| 2 | **2026-08-16 の 39 件は対象外**（週の範囲外として除外） | 最新日が未解析 |
| 3 | §8.2.1 の結論を**本組織で再現していない**。1,650 回の実測は著者の環境・著者のルール文書によるもの | 候補 AO の根拠は他者の測定に依存 |
| 4 | **§8.5 #2 の重複が「矛盾」かどうかを判定していない**。同一キーワードが複数ファイルに出ることと、内容が食い違うことは別 | 候補 AO の作業量が見積もれない |
| 5 | 記事自身が「**矛盾した文章が混ざることの害は測っていない**」と明記。整合性仮説は未検証 | 候補 AO は仮説に基づく組み替えであり、効果は保証されない |
| 6 | §8.2.3 の数値は**すべて Anthropic 自身の発表**。第三者による再現は未確認 | auto mode の 89% を鵜呑みにできない |
| 7 | **auto mode が実際に本セッションで動いているかを確認していない**（`defaultMode` 未指定から推定しただけ） | 候補 AR の前提が推定 |
| 8 | fork 既定化後の**トークン消費を本組織で測っていない**。本編 §2.1 の 44% 削減が fork 前提でどう変わるかは不明 | 候補 AQ の効果は未知 |
| 9 | §8.4.1 §8.4.2 の仕組みを**動かしていない**（設定例を読んだだけ） | 候補 AP / AS の実装コストは未見積もり |
| 10 | 候補 **AO〜AU はいずれも未実施** | — |

---

## 9. 出典一覧（精読 20 件）

| # | 記事 | 出典 | 取得 |
|---|---|---|---|
| 1 | [検証！Claude Code のサブエージェント並列はお得なのか](https://qiita.com/tamashiro_nobuyuki/items/ff9004b66b7761c4d34c) | Qiita | curl 3,621 字 |
| 2 | [【並列開発】isolation: worktree やめました](https://zenn.dev/rakko_inc/articles/2da36c0abe4dbe) | Zenn | curl 6,973 字 |
| 3 | [プロンプトキャッシュを意識して claude code のトークンを節約しよう](https://zenn.dev/ceres_tech_blog/articles/7163ae482e124e) | Zenn | curl 5,931 字 |
| 4 | [Claude Code の「無駄」を可視化するツール cclens を作った](https://zenn.dev/lambdalisue/articles/introduce-cclens) | Zenn | curl 8,824 字 |
| 5 | [【Claude Code】plan モードはもう使っていない](https://zenn.dev/haru067/articles/0c28638945aa32) | Zenn | curl 1,974 字 |
| 6 | [【決着】Claude Code と Codex の設定ファイルを同期させる（みんな仲良く）](https://zenn.dev/kanta_tkd/articles/20f7f41cff8428) | Zenn | curl 3,413 字 |
| 7 | [Amazon Bedrock AgentCore Gateway で Codex の利用量と Budget を制御する](https://zenn.dev/kashiwabaray/articles/796539362435eb) | Zenn | curl 12,971 字 |
| 8 | [Claude Code のようなエージェントをゼロから作る](https://zenn.dev/sun_asterisk/articles/f5514175aa0a0a) | Zenn | curl 18,827 字 |
| 9 | [AI エージェント向けのツールを、MCP ではなく CLI として作った — 設計の 3 原則](https://zenn.dev/codatum/articles/80b99faba75704) | Zenn | curl 13,163 字 |
| 10 | [「純粋リファクタリング」を AI に証明させて自動マージする](https://engineering.nature.global/entry/2026/07/prove-pure-refactoring-with-ai-and-auto-merge) | Nature Engineering Blog | curl 5,756 字 |
| 11 | [コーディングエージェントを安全に使うための実務ガイド v0.2](https://zenn.dev/kanaria007/articles/ee3dfd438af4df) | Zenn | curl 9,713 字 |
| 12 | [40 万件の AI 承認を分析したら、見逃し率が 3 倍違った](https://qiita.com/jqit_suwa/items/ac7d1201bd14e9a4e1ac) | Qiita | curl 4,422 字 |

追補（§8、2026-08-10〜08-15）:

| # | 記事 | 出典 | 取得 |
|---|---|---|---|
| 13 | [「CLAUDE.md は短い方が良い」を 1,650 回試して確かめた](https://zenn.dev/odacchi/articles/claude-md-length-experiment) | Zenn | curl 8,619 字 |
| 14 | [Claude Code v2.1.230〜v2.1.232 の主要アップデート](https://dev.classmethod.jp/articles/20260814-cc-updates-v2-1-232/) | DevelopersIO | curl 13,882 字 |
| 15 | [Claude Code、承認ボタンを何度も押さなくていい「自動モード」がデフォルトに](https://pc.watch.impress.co.jp/docs/news/2131930.html) | PC Watch | curl 9,798 字 |
| 16 | [Claude Code の worktree フックで作る使い捨て隔離開発環境](https://zenn.dev/cilly/articles/claude-code-worktree-isolated-env) | Zenn | curl 9,857 字 |
| 17 | [Claude Code経由でCodexを使う「claudex」の導入・検証レポート](https://zenn.dev/yam2357/articles/claudex-proxy-codex) | Zenn | curl 13,640 字 |
| 18 | [Claude Codeに「違反できないコード」を書かせる：ガイドとセンサーの設計](https://qiita.com/shun123/items/7aa06b6982dfd1730af0) | Qiita | curl 34,401 字 |
| 19 | [Claude Code に「同じ指摘を二度させない」仕組みを hook で作った](https://zenn.dev/nozomi720/articles/claude_code_hooks_feedback) | Zenn | curl 9,506 字 |
| 20 | [Agent Plugins 1.0.0 は何を標準化し、何を標準化しなかったのか](https://zenn.dev/inariku/articles/agent-plugins-1-0-deep-dive) | Zenn | curl 17,364 字 |

---

## 関連

解析レポートは **GitHub Pages 配信側（`docs/insights/analyses/`）**、採用判断カタログと入口 README は**組織スコープ側**（`.companies/domain-tech-collection/docs/insights/`）にある。

- [`catalog.md`](../../../.companies/domain-tech-collection/docs/insights/catalog.md) — 採用判断カタログ（本レポートの知見を §5.9〜5.12 として追記。候補 AD〜AN）
- [`2026-08-digest-ai.md`](2026-08-digest-ai.md) — 8/1〜8/8 の解析（CLAUDE.md 削減 / Skill 弁別性 / 品質保証の分担）
- [`ai-pr-approval-gate.md`](ai-pr-approval-gate.md) — AI による PR 承認ゲート
- [`task-management.md`](task-management.md) — Claude Code / Codex のタスク管理
- `@.claude/rules/web-content-fetch.md` — 本レポートの取得方法の根拠

> **配置についての注記**: `@.claude/rules/artifact-placement.md` は業務ドキュメント（MD）の配置先を `.companies/{org}/docs/` と定めており、`analyses/` 配下はその原則の例外にあたる。オーナー判断（2026-08-16）で全 7 本を Pages 配信側へ移した。`docs/requirements.md` や `docs/guide/*.md` と同じ扱い。**ルールファイル側は未更新**。
